# LoopCAT 0.0.3 — historical prerelease and superseded mirror

This is a historical record, not the current download guide. The current unsigned development preview is [0.0.4-dev.20260831](0.0.4-dev.20260831.md); its exact source snapshot and hashes are in [downloads/release.json](../../downloads/release.json).

The local `draft-0.0.3` tag points to commit `6f9754d` dated 16 July 2026. The [historical GitHub prerelease](https://github.com/gokhandogru/LoopCAT/releases/tag/draft-0.0.3) is a separate provenance boundary. Later untagged repository mirror builds also used version 0.0.3, which made filenames ambiguous. Those mirror downloads have now been superseded; the historical tag and external assets have not been changed.

The Loopbird branding was introduced on 7 August at `0a2d534`, after the July tag. It must not be attributed to the original tagged prerelease.

## July Tagged-Prerelease Highlights

- Adds XLIFF 2.2 Core import, generic handoff export, and current-file target reconstruction.
- Keeps XLIFF 2.0 and 2.1 Core compatibility.
- Supports Core hierarchy, notes, state, whitespace, `originalData`, and `cp`/`ph`/`pc`/`sc`/`ec`/`mrk`/`sm`/`em` inline content.
- Preserves skeletons, ignorable content, custom attributes, and Part 2/custom extension XML when rebuilding imported files.
- Adds separate XLIFF 1.2 and XLIFF 2.2 export actions and uses `application/xliff+xml` for XLIFF 2.x downloads.
- Adds offline OASIS-schema validation for a fixture and a live generated 2.2 handoff.

## Later August Repository-Mirror Additions (superseded)

- Introduces the borderless Loopbird logo, shared web/PWA/desktop icon assets, the LoopCAT brand palette, and a reproducible icon-generation command.

- Upgrades the desktop runtime to Electron 43 with renderer sandboxing, secure fuses, hardware acceleration by default, and an explicit restart-based graphics fallback.
- Separates production and test renderer graphs and reduces root `app.js` to a 13-line bootstrap into checked feature controllers, services, stores, and lazy first-use boundaries.
- Adds semantic System/Light/Dark themes, Balanced/Compact density, a responsive remembered contextual inspector, Focus mode, and a keyboard-complete grouped command palette.
- Adds bounded Undo/Redo plus local Trash for reversible target, review, AI, pretranslation, structural, project, document, TM, term, and resource workflows.
- Adds separated save/job/notice/error presentation, actionable update activation, local-only redacted diagnostics, actionable empty states, DOM/Trusted Types hardening, and expanded Catalan/English/Turkish interface localization.
- Preserves the mature offline, format, package, workspace, QA, reporting, AI consent/redaction, and delivery safeguards under the full automated regression gate.

## Superseded Repository Download Names

- `LoopCAT.Web.0.0.3.zip` — static web application.
- `LoopCAT.Windows.Setup.0.0.3.zip` — unsigned Windows installer containing `LoopCAT Setup 0.0.3.exe`.
- `LoopCAT.0.0.3.Portable.zip` — unsigned portable Windows application containing `LoopCAT 0.0.3.exe`.
- `LoopCAT.0.0.3.SHA256SUMS.txt` — SHA-256 checksums for the three ZIP files.

These filenames identify the superseded untagged 0.0.3 mirror, not the current download set. Do not use its old checksum list with the new preview. See [`downloads/README.md`](../../downloads/README.md) for the current version.

## Historical Verification Record

The following checks were reported during the earlier July/August work. They are not a claim that every old 0.0.3 ZIP contains the latest keyboard fixes, and they are not fresh results for the new preview.

- Release contract, i18n validation, and catalog compilation.
- OASIS XLIFF 2.2 Core schema validation.
- Full hidden-Electron browser suite: security policy, offline shell, smoke, regression, app workflow, workspace storage, package round trip, and large project.
- Static web artifact verification and desktop/mobile rendered smoke checks.
- Packaged Windows desktop smoke and `app.asar` payload verification.
- Public ZIP naming, archive size, and SHA-256 checksum verification.
- Modernization quality gate: 1,364 focused/static tests, eight browser phases, 15 deterministic accessibility states, and 81 visual checkpoints.
- Production/test isolation, renderer bundle contract, Electron sandbox/GPU startup, artifact payload, and security-fuse verification.

## Release Qualification Outstanding at That Checkpoint

- Windows Authenticode signing and clean-machine evidence remain incomplete.
- Native CI has built and verified Linux AppImage/DEB artifacts, but checksum-authenticated clean-machine install/launch evidence is still required before formal publication.
- Current Windows and macOS release candidates still require external signing credentials; macOS additionally requires notarization and stapling. Every public artifact still needs recorded clean-machine evidence.
- LoopCAT preserves optional XLIFF Part 2 modules and custom extensions during current-file reconstruction, but it does not yet expose module-specific editing or author every Part 2 module in generic exports.
