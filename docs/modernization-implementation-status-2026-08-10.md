# LoopCAT modernization implementation status

Status date: 2026-08-14
Baseline commit: `0a2d5343d6c2a2ef859a16bc0674e128e7803d1a`
Implementation state: substantial P0-P3 delivery, with target editing, autosave, their direct application/workflow consumers, segment confirmation, discrete target producers and direct consumers, target replacement and checked editor-controller direct consumers, TM and local/hosted AI batch pretranslation, active/batch AI review, protected-tag repair, target alternatives, terminology application/extraction and candidate persistence, draft polish/adaptation, project-brief generation, direct OpenAI suggestion requests and resource-context composition, shared AI suggestion persistence, suggestion-list presentation, segment context, scope/sample selection, external-send consent, provider presentation, browser credential storage, runtime-settings composition/readiness, provider-form/command-centre orchestration, command-time local-settings persistence, shared AI command lifecycle/cancellation, prompt-only AI lifecycle adapters, prompt-preview compatibility-façade cleanup, AI command-dispatch direct wiring, suggestion/context consumer-façade cleanup, and provider/help consumer-façade cleanup, project AI-settings persistence, local-provider connection/model administration, AI prompt preview/testing, AI suggestion application, quick/full review metadata, quality decisions, quality profiles, and structural split/merge orchestration and direct consumers extracted and the remaining P1-08 coordinator work plus the performance stretch target still partial

This document records what the current working tree implements from the 2026 modernization roadmap. It supplements, and does not replace, the review and plan. The repository must not be described as having completed the entire roadmap until the partial items below and the remaining manual/platform gates are closed.

## Delivered

- Deterministic characterization fixtures, bundle/version-contract enforcement, three-viewport screenshot capture, accessibility automation, and focused unit/static gates.
- Separate esbuild production/test renderer graphs. Production verification rejects workflow harnesses, mocks, fixtures, debug routes, source test globals, and test imports.
- One generated production asset manifest used by renderer staging, web packaging, desktop packaging/protocol allowlisting, service-worker caching, and artifact verification.
- Electron 43.3.0, electron-builder 26.15.3, Node 24 tooling, renderer sandboxing, context isolation, secure fuses, hardware acceleration by default, and an explicit restart-based GPU fallback.
- Immediate accessibility repairs for live status, focus visibility, modal focus containment/restoration, disclosure state, and command-palette keyboard behavior.
- A checked shared dialog-lifecycle controller now owns open, close, native cancel, initial focus, and explicit focus return for About, Diagnostics, Trash, the TM pretranslation threshold prompt, and OPUS-CAT connection help. Feature-local adapters return only user intent: TM matching and provider/network ownership remain in their existing services. TM cancel returns to the visible Segment tools trigger; OPUS-CAT help returns to its visible Project settings entry point. The superseded `showManagedDialog` helper and direct TM/OPUS lifecycle listeners are removed.
- A checked project-dialog controller owns create/edit mode, asynchronous resource preparation, field and delegated dynamic listeners, workspace-folder interaction, AI-settings deep linking, and lifecycle delegation. The existing domain save service remains the only persistence/validation boundary, and no project/resource/workspace data moved into UI state.
- A checked Resources controller now owns Resources navigation, TM/termbase tab state and keyboard behavior, view-local resource selection, language-field normalization, import-input lifecycle, card/detail/row event delegation, initial focus, and focus restoration across asynchronous resource refreshes. Existing parsers, storage, exports, validation, linked-project dirtiness, and recovery boundaries remain injected domain services. Empty Resources actions now open the dedicated resource import inputs instead of the project-level import inputs.
- A checked quality/review controller now owns Comments and Quality Workbench rendering, review/profile/decision form values and event lifecycles, delegated risk navigation, in-progress form preservation, deterministic empty states, and focus restoration across risk-list rendering. Quality scoring, QA, persistence, commands, status/activity, workspace dirtiness, AI review safeguards, and Quality Passport/report generation remain injected domain boundaries.
- A checked recovery/workspace controller now owns workspace health and local-storage presentation, connected/unsupported/busy control states, project-folder status, recovery and backup-reminder rendering, workspace action listeners, recovery dismissal, menu access, cleanup, and visible focus restoration when recovery UI disappears. Directory handles, workspace manifest v1, project-package schema 5, database/backup schema 6, dirty-marker persistence, pending-save flushes, autosave, package validation/import conflict policy, repair, redaction, jobs, and status remain injected domain boundaries. The recovery-folder action now stops the application outside-click handler from immediately closing the menu.
- A checked import/export controller now owns project-file, project-package, browser-backup, TMX, TBX, term-list, delivery, project-report, Quality Passport, and anonymized-report event lifecycles; sequential multi-file input reset; one shared import busy state; safe validation-report DOM; delegated dismissal; auto-dismiss timing; and visible focus restoration. Parsers, encoding detection, format reconstruction, package/backup replacement, pending-save flushes, report generation, downloads, validation/redaction policy, activity history, workspace dirtiness, storage, and status remain injected application boundaries.
- A checked AI administration controller now owns global and project provider-form values, provider preset/model options, safe provider-summary DOM, privacy/status/progress/busy presentation, prompt previews, output disclosure, static contextual/command-centre action listeners, language-field event lifecycle, and cleanup. Provider registries/adapters, endpoint allowlists, credential storage, prompt construction, external-send consent, batches/cancellation, suggestions, commands/Undo, provenance, project persistence, activity history, redaction, and status/error decisions remain injected application boundaries.
- A checked immutable compatibility-module registry now makes the runtime installer the single bridge from the legacy `window.CatHan` module namespace into the application runtime. `app.js` reads only `window.CatHan.appRuntime`; storage, projects, AI, encoding, format handlers, workspace storage, i18n, focus, workers, QA, validation, analysis, and quality APIs are injected through the runtime. Release verification rejects direct feature-global access returning to the coordinator.
- AppStore is now the sole writer for application route, selected project/document identity, active segment identity, and Focus mode. Editor filters are owned by one checked FilterStore, while a checked EditorSessionStore owns project/session record replacement through explicit selectors and actions; the temporary compatibility bridge has been removed.
- The project-summary cache and its dirty-revision map are the first EditorSessionStore family removed from that bridge. Dashboard reads, replacements, copy-on-write dirty increments, stale-revision pruning, and workflow fixtures now use explicit checked store APIs; release verification rejects either legacy `state` property returning.
- Project-term and activity-event contextual caches are also removed from the EditorSessionStore compatibility bridge. Project switching and terminology refresh use explicit checked replacement APIs, while activity logging uses a newest-first deduplicated prepend action; reports, backup reminders, workspace persistence, and workflow characterization read through checked selectors.
- QA-check and quality-risk contextual caches are now removed from the EditorSessionStore compatibility bridge. Successful QA and report generation use explicit checked replacement actions, failed QA preserves the previous checks, and Quality Workbench rendering, risk navigation, profile/decision refreshes, reports, and workflow characterization read through checked selectors.
- The derived project progress-summary cache is now removed from the EditorSessionStore compatibility bridge. Progress rendering reads and replaces the cache through checked APIs while preserving its project/segment-count validity guard, incremental confirmed-status delta, single-pass fallback, cached source word counts, and existing UI output.
- The core project collection and current-project record are now removed from the EditorSessionStore compatibility bridge. Loading, creation, switching, settings edits and rollback, imports, project-package history, Trash/Undo, recovery, workspace sync, reports, AI context, and workflow fixtures use explicit checked selectors and replacement actions.
- The segment collection is the final family removed from the EditorSessionStore compatibility bridge. Loading, imports, project/package restore, commands and Undo/Redo, autosave/retry, pretranslation, QA/review, document filtering, virtualization, structural split/merge, and workflow fixtures read through the checked selector; whole-collection and indexed replacements use explicit actions, with indexed replacement preserving copy-on-write identity. The temporary `attachCompatibility` API is deleted and release verification prevents it or direct legacy `state.segments` access from returning.
- A checked SegmentGridController now owns the virtual window, coalesced scroll and row-update frames, active-segment dispatch, visibility scrolling, and focused target-editor lookup. A checked EditorContextController coordinates Matches, Terms, Review, History, AI suggestions, and Quality refreshes while their domain services and renderers remain injected.
- A checked TargetEditController now owns target-textarea focus/blur, input/composition, keyboard Undo/Redo and confirmation/navigation routing, caret lookup/normalization/restoration, and EditTarget session coalescing. A checked AutosaveService owns the 450 ms pending-save queue, latest-session-record selection, 2 s retry, project/document clearing, forced flush recovery, and repository sequencing. EditorSessionStore, CommandBus, repository, status, rendering, workspace dirtiness, and domain-record mutations remain injected boundaries; discrete source/match/tag producers, structural split/merge, and bulk changes remain separate.
- A checked SegmentConfirmationController now owns the Confirm button lifecycle, per-segment busy state, protected-tag blocking, CommandBus sequencing, confirm-and-next navigation, filter/progress/history rendering decisions, primary failure recovery, persisted monotonic rollback, and non-transactional TM/activity warning results. EditorSessionStore records, domain mutations, repository writes, TM/activity services, status, workspace dirtiness, and command restoration remain injected boundaries; ordinary target editing, discrete source/match/tag producers, structural split/merge, review-state changes, and batch pretranslation remain separate.
- A checked TargetProducerController now owns the Copy Source button lifecycle and shared Copy Source, TM match/concordance insertion, and protected-tag insertion orchestration: pending EditTarget finalization, patch/caret capture, redacted command execution, provenance, filter-sensitive rendering, autosave scheduling, workspace dirtiness, success status, and exact failure rollback. EditorSessionStore records, CommandBus implementations, domain mutations, AutosaveService, command restoration, rendering, status, and selection remain injected boundaries; ordinary typing, confirmation, structural split/merge, review-state changes, and batch pretranslation remain separate.
- Target restoration now calls TargetEditController selection normalization directly, while command-palette, TM-match, concordance, protected-tag UI, and workflow consumers call TargetProducerController public methods directly. The four target selection/producer forwarding functions are removed with omitted options, promise/results, provenance channels, caret placement, failure containment, and Undo/Redo behavior preserved; release verification prevents them from returning.
- A checked TargetReplacementController now owns the visible/project-wide replacement button lifecycle, input and filter-option reads, protected-token proposal/count construction, pending-save flush, ReplaceTargets command creation/execution, snapshot and Undo/Redo restoration coordination, atomic batch save, target history/status mutation, best-effort activity logging, tag-QA warning status, presentation refresh, focus recovery, and exact primary-failure rollback. EditorSessionStore records, filter indexes, protected-token replacement, CommandBus implementation, AutosaveService, repository, mutation/history adapters, command restoration, selection, presentation, activity, and status remain injected boundaries; ordinary typing/autosave, discrete target producers, confirmation, structural edits, quality/review saves, and batch pretranslation remain separate.
- A checked TmPretranslationController now owns the Pre-translate button lifecycle, threshold/re-entry/busy state, empty/confirmed/locked eligibility safeguards, deduplicated batched TM lookup with progress yielding, threshold proposal filtering, pending-save flush, patch/snapshot capture, redacted atomic command execution, normalized TM provenance with stale AI provenance clearing, batch persistence, best-effort activity/sidebar refreshes, workspace/status/presentation updates, exact primary-failure rollback, and unconditional busy cleanup. EditorSessionStore records, threshold dialog, TM lookup, CommandBus implementation, AutosaveService, repository, mutation/history adapters, command restoration, selection, presentation, activity, workspace, and status remain injected boundaries; manual target editing/replacement, local/hosted AI pretranslation, confirmation, structural edits, and quality/review saves remain separate.
- Target-replacement workflow, target-editor and discrete-producer autosave scheduling, active-segment confirmation-busy refresh, confirmation command-palette/workflow actions, and TM-pretranslation workflow consumers now call checked public methods directly. The TM-threshold dependency retains one exact late-bound adapter with its unavailable-browser error, visible Segment tools focus-return target, and promise/result preserved. The six replacement/autosave/confirmation/TM forwarding functions are removed, and release verification prevents them from returning.
- All remaining application, controller, and workflow target-edit/autosave consumers now call TargetEditController and AutosaveService public methods directly. The confirmation controller retains one exact call-time focus adapter because it is composed before TargetEditController assignment; all later consumers bind directly. Project-versus-global EditTarget finalization, pending-save clear options/defaults, inspection, flush results/rejections, draft-update returns, focus/caret results, retry scheduling, and Undo/Redo behavior remain unchanged. The nine `finalizePendingEditCommand`, `finalizePendingEditCommands`, `clearPendingSave`, `clearAllPendingSaves`, `pendingSaveRecords`, `clearPendingDocumentSaves`, `flushPendingSegmentSaves`, `updateSegmentDraft`, and `focusActiveTextarea` façades are removed, and release verification prevents them from returning.
- A checked AiPretranslationController now owns local/hosted AI runtime validation, unavailable-provider handling, external-send and overwrite consent, pending-save flush, scope/eligible-segment selection, progress/running/AbortController lifecycle, provider-domain invocation and prompt-context routing, mid-batch cancellation rollback, target history/revision preparation, redacted atomic command persistence, best-effort activity/repository/sidebar refreshes, workspace/status/presentation updates, exact primary-failure rollback, and unconditional lifecycle cleanup. EditorSessionStore records, provider adapters/registry, settings and prompt/context services, CommandBus implementation, repository, mutation/history adapters, command restoration, selection, presentation, activity, workspace, and status remain injected boundaries; AI administration, manual/TM target changes, confirmation, structural edits, quality/review saves, and the other AI review/edit command families remain separate.
- A checked AiReviewController now owns active-segment and batch AI-review eligibility, runtime/provider validation, external-send consent, glossary context routing, risk normalization, stable attributed comments, visible/document/project/selected scope selection, locked/confirmed/source/target safeguards, progress/running/prompt/AbortController lifecycle, per-segment failure containment, partial-result cancellation, persistence/reload sequencing, best-effort activity logging, presentation/workspace/status updates, exact primary-failure rollback, and final cleanup. EditorSessionStore records, provider adapters/registry, settings and prompt construction, repository, mutation/history adapters, selection, presentation, activity, workspace, status, labels, IDs, clocks, and redaction remain injected boundaries; manual review/quality decisions, pretranslation, tag repair, target editing, confirmation, and structural edits remain separate.
- A checked AiTagRepairController now owns active-segment and batch protected-tag-repair eligibility, missing-tag detection, runtime/provider validation, external-send consent, protected-token routing, visible/document/project/selected scope selection, locked/confirmed/source/target safeguards, normalized non-overwriting suggestion construction, progress/running/prompt/AbortController lifecycle, per-segment failure containment, partial-result cancellation, persistence/reload sequencing, best-effort activity logging, presentation/workspace/status updates, exact primary-failure rollback, and final cleanup. EditorSessionStore records, provider adapters/registry, settings and protected-token prompt services, repository, mutation/history adapters, general suggestion storage, selection, presentation, activity, workspace, status, IDs, and redaction remain injected boundaries; target editing, confirmation, pretranslation, review, alternatives, and structural edits remain separate.
- A checked AiAlternativesController now owns active-segment and batch target-alternatives eligibility, runtime/provider validation, external-send consent, glossary/protected-token routing, variant-mode propagation, empty/current-target duplicate filtering, normalized multi-suggestion construction, visible/document/project/selected scope selection, locked/confirmed/source/target safeguards, progress/running/prompt/AbortController lifecycle, per-segment failure containment, partial-result cancellation, persistence/reload sequencing, best-effort activity logging, presentation/workspace/status updates, exact primary-failure rollback, and final cleanup without overwriting target text. EditorSessionStore records, provider adapters/registry, settings and prompt/context services, repository, mutation/history adapters, general suggestion storage, selection, presentation, activity, workspace, status, IDs, and redaction remain injected boundaries; target editing, confirmation, pretranslation, review, tag repair, terminology application, and structural edits remain separate.
- A checked AiTerminologyApplicationController now owns active-segment and batch terminology-application eligibility, runtime/provider validation, no-term short-circuiting, external-send consent, glossary/protected-token routing, unchanged-result filtering, normalized non-overwriting suggestion construction, visible/document/project/selected scope selection, locked/confirmed/source/target safeguards, progress/running/prompt/AbortController lifecycle, per-segment failure containment, partial-result cancellation, persistence/reload sequencing, best-effort activity logging, presentation/workspace/status updates, exact primary-failure rollback, and final cleanup. EditorSessionStore records, provider adapters/registry, settings and prompt/context services, repository, mutation/history adapters, general suggestion storage, selection, presentation, activity, workspace, status, IDs, and redaction remain injected boundaries; target editing, confirmation, pretranslation, review, tag repair, alternatives, draft polish/adaptation, and structural edits remain separate.
- A checked AiDraftEditingController now owns active-segment and batch polish/adaptation eligibility, operation-specific runtime/provider validation and external-send consent, TM/termbase/style/protected-token context routing, adaptation-mode propagation, unchanged-result filtering, normalized non-overwriting suggestion construction, visible/document/project/selected scope selection, locked/confirmed/source/target safeguards, progress/running/prompt/AbortController lifecycle, per-segment failure containment, partial-result cancellation, persistence/reload sequencing, operation-specific best-effort activity logging, presentation/workspace/status updates, exact batch primary-failure rollback, and final cleanup. EditorSessionStore records, provider adapters/registry, settings and prompt/context services, repository, mutation/history adapters, general suggestion storage, selection, presentation, activity, workspace, status, IDs, and redaction remain injected boundaries; target editing, confirmation, pretranslation, review, tag repair, alternatives, terminology application/extraction, and structural edits remain separate.
- A checked AiTerminologyExtractionController now owns active-segment and batch term-extraction eligibility, runtime/provider validation, external-send consent, selected/visible/project/document/untranslated source scope, provider progress/running/prompt/AbortController lifecycle, per-segment failure containment, partial-result cancellation, extracted-term aggregation, injected deduplication/storage sequencing, active/batch activity logging, best-effort resource/sidebar refreshes, presentation/workspace/status updates, and final cleanup. EditorSessionStore records, provider adapters/registry, settings and prompt services, term normalization/deduplication/storage, termbase selection, repositories, activity, workspace, status, and presentation remain injected boundaries; target editing, terminology application, project-brief generation, and other AI commands remain separate.
- A checked AiProjectBriefController now owns project eligibility, local/hosted AI settings persistence and runtime/provider validation, external-send consent after bounded document/sample/term context selection, provider prompt/running lifecycle, generated-brief normalization, project/list snapshots, append-only style-guide mutation, project persistence and synchronization, administration-form refresh, best-effort activity logging, output/workspace/status updates, exact primary-failure rollback, and final cleanup. EditorSessionStore records, provider adapters/registry, settings and prompt construction, document/sample/term context selectors, project repository, AI administration, activity, workspace, status, and presentation remain injected boundaries; target editing, terminology commands, pretranslation, suggestion application, and direct OpenAI suggestion requests remain separate.
- A checked AiSuggestionApplicationController now owns active-segment and suggestion eligibility, confirmed/locked safeguards, pending-save flushes, ApplyAiSuggestion command construction/execution, first-apply target/status/provenance/review mutation, repository persistence, best-effort activity logging, apply-and-next navigation, Undo/Redo snapshot restoration with monotonic revisions, sidebar and editor presentation, workspace/status updates, exact primary-failure rollback, and restoration-failure recovery. EditorSessionStore records, suggestion records, CommandBus implementation, target/history mutation adapters, repository, selection, presentation, activity, workspace, and status remain injected boundaries; suggestion creation/storage, direct OpenAI requests, provider adapters, AI administration, ordinary target editing, and batch AI commands remain separate.
- A checked AiOpenAiSuggestionController now owns direct OpenAI project/segment and settings eligibility, the offline-before-key safeguard, explicit external-send consent with optional local-context disclosure, project/list/key snapshots, settings persistence and list synchronization, key-memory policy, setup rollback with rollback-write warnings, TM/termbase context and provider request routing, shared suggestion-storage result handling, presentation/workspace/status updates, and request-failure containment. EditorSessionStore records, AI administration, settings normalization, OpenAI transport, context lookup, key storage, project repository, shared suggestion persistence, presentation, workspace, and status remain injected boundaries; general suggestion normalization/storage, suggestion application, local/hosted AI commands, and provider adapters remain separate.
- A checked AiSuggestionPersistenceController now owns the portable/redacted suggestion storage shape, default IDs/provider/status/origin/scope/review-state/date normalization, finite confidence, bounded explanation/context disclosure, append-only segment mutation, revision/history and pending-save sequencing, repository persistence, best-effort activity logging, suggestion/history presentation, workspace/status outcomes, activity-warning containment, and exact primary-failure rollback. Segment records, repository implementation, mutation/history adapters, redaction, IDs, clocks, activity, presentation, workspace, and status remain injected boundaries; provider requests, producer-specific orchestration, direct OpenAI setup, suggestion application, EditorSessionStore, and AI administration remain separate.
- A checked AiSettingsPersistenceController now owns global/local form and secret reads, normalized project AI settings with local-provider fields, endpoint validation, OpenAI/local credential update eligibility, project/list/key snapshots, project repository sequencing, selected-project/list synchronization, credential-memory policy, activity storage labels, best-effort activity warnings, editor/workspace/status updates, exact project/list/key rollback, and rollback-write warnings. Form state, settings normalization, local project-field mapping, endpoint policy, provider identity, key stores, EditorSessionStore records, project repository, activity, presentation, workspace, and status remain injected boundaries; provider requests, provider connection/model administration, suggestion commands, and target editing remain separate.
- A checked AiProviderAdministrationOperationsController now owns local/hosted provider connection validation and invocation, connection/status copy, OPUS-CAT discovered-endpoint persistence and setup-help routing, desktop LM Studio eligibility/start/retry, model discovery and selected-model availability decisions, Ollama pull eligibility/progress/refresh, and provider-error containment. Provider adapters/registry, settings persistence/runtime policy, endpoint normalization, desktop bridge, model records, AI administration presentation, project records, and status stores remain injected boundaries; prompt testing, batch commands, credential storage, and project AI-settings persistence remain separate.
- A checked AiPromptTestController now owns single-prompt project/running/busy/source eligibility, settings/runtime/provider and mode-capability validation, mode-aware external-share consent, translation-versus-generic provider routing, exact request payload selection, prompt-busy lifecycle, output normalization, command-centre/output presentation, status decisions, provider-error containment, and unconditional busy cleanup. Prompt construction and contextual labels, provider adapters/registry, settings persistence/runtime policy, project/sample records, AI administration presentation, batch commands, and connection/model administration remain injected boundaries.
- A checked AiPromptPreviewController now owns every prompt-mode label/system/consent-context mapping, explicit sample versus active-source fallback, preview-segment/tag synthesis, matching-term fallback with the 12-term bound, protected-token/style/variant/adaptation/empty-TM context, optional nearby segments, project-brief document/sample/term context, command-specific prompt-builder routing, request metadata, and preview rendering. AI administration form state, project/session/term/document records, prompt builder implementations, provider execution, settings persistence/runtime policy, and status remain injected boundaries.
- A checked AiTermCandidatePersistenceService now owns existing-term lookup for the active language pair/termbase, stable case-insensitive source/target deduplication against stored and repeated candidates, candidate note/forbidden-state construction, ordered sequential saves, saved-record aggregation, linked-project dirtiness after successful completion, and duplicate counts. Term repository implementation, project/resource records, language normalization, terminology provider/batch orchestration, presentation, activity, workspace storage, and status remain injected boundaries; partial save failure still propagates before linked-project dirtiness exactly as before.
- A checked AiSegmentContextService now owns project-level termbase/TM context eligibility, exact language/resource/source/limit lookup parameters, existing warning containment, settings/segment fallbacks, and ordered nonblank nearby-segment selection from the active document. Project/resource records, settings normalization, term/TM repository implementations, segment storage, AI command controllers, presentation, and status remain injected boundaries.
- A checked AiScopeSelectionService now owns selected/visible/project/untranslated/default terminology scopes, project-versus-document pretranslation sources, ordered selected/visible stable-ID options, and bounded project-brief samples with active-document preference, project fallback, blank-source filtering, and empty-target fallback. Project/session records, form settings, filter computation, AI providers, command controllers, presentation, and status remain injected boundaries; AiTerminologyExtractionController consumes the shared selection instead of duplicating it.
- A checked ExternalAiConsentService now owns stable trimmed/deduplicated disclosure labels, zero/one/two/Oxford-comma list grammar, mandatory project-instruction and optional selected/source-text disclosure order, exact provider-aware confirmation copy, and unchanged accepted/canceled result propagation. The confirmation primitive, provider selection, command eligibility/timing, controllers, presentation, and status remain injected boundaries; all ten local/hosted and direct OpenAI command families consume the service.
- A checked AiProviderPresentationService now owns local, Ollama-cloud, hosted-key, and network privacy copy; safe endpoint-path reduction; exact model/translation endpoint summaries for all 18 provider registry positions; model-pull eligibility; ordered capability labels and fallback; localized badge order; and provider/preset/model/base/guidance/nearby-context summary fallbacks. Provider adapters and registry lookup, preset and URL builders, locality/key/guidance policy inputs, localization primitives, credential storage, administration rendering, commands, and status remain injected boundaries.
- A checked AiCredentialStorageService now owns browser-only global OpenAI and provider/base-scoped local/hosted AI key access; safe local/session storage acquisition, read/write/clear warning containment, session-before-local key precedence, remember-versus-tab routing, blank-key clearing, legacy-key cleanup, exact snapshots/restoration, primary-failure rollback, safe-restore warnings, and storage labels. Browser storage objects, settings and provider/base normalization, test-failure injection, administration presentation, redaction, status, project persistence, and command orchestration remain injected boundaries. AI settings, direct OpenAI suggestions, provider controls, and workflow characterization consume this service, and release verification rejects the coordinator-private storage helpers returning.
- A checked AiRuntimeSettingsService now owns normalized global project AI settings; redacted provider/model/style/local metadata; local mode allowlists and concurrency/timeout bounds; local-provider project/form/language composition; hosted OpenAI-compatible endpoint eligibility; typed, provider-scoped, and OpenAI-global credential selection; and provider-key readiness with action-specific copy. The administration controller, local settings store, language and provider primitives, endpoint allowlist predicate, credential service, project records, redaction primitive, presentation, persistence, status, and commands remain injected boundaries. Every AI command/controller consumes the checked methods, and release verification rejects the five former coordinator settings/runtime helpers returning.
- A checked AiProviderFormController now owns provider preset grouping and selection; provider, model, progress, output, and full command-centre view-model composition; provider/preset/base-URL/general-form/language transitions; live language-field synchronization; key-clear intent routing; redacted connection status; and exact presentation refresh sequencing. The AI administration controller retains DOM and listener ownership, while provider adapters/registry, runtime-settings and credential policy, provider presentation, project/session records, OPUS-CAT help, persistence, status storage, localization, and AI commands remain injected boundaries. Release verification rejects the former coordinator-private provider-form render and transition helpers returning.
- A checked AiSuggestionListController now owns the localized muted empty state; bounded newest-first four-card composition; provider/model/confidence fallbacks; safe provenance, before/after inspection, and explanation DOM; and separate Apply versus Apply and next intent routing. Suggestion creation/normalization/storage, application commands and Undo/Redo, target/history mutation, persistence, navigation, project/session records, localization/date primitives, workspace, status, and producer-specific AI requests remain injected boundaries. All suggestion producers, application refreshes, and the contextual inspector consume the checked renderer, and release verification rejects suggestion-list DOM or button ownership returning to `app.js`.
- AI suggestion and segment-context consumers now route directly to the checked AiSuggestionPersistenceController, AiSuggestionApplicationController, and AiSegmentContextService public methods. Suggestion-list and producer consumers created before those controllers retain exact rest-argument late binding, while later controllers and workflow characterization call the public boundaries directly. The six compatibility forwarders for suggestion normalization, append, application, glossary terms, TM matches, and nearby segments are removed, and release verification prevents them from returning.
- AI command consumers now resolve configured providers directly through AiProviderService while preserving the call-time local-form fallback; the provider form late-binds LM Studio eligibility directly to AiProviderAdministrationOperationsController; and provider-form, administration, and workflow consumers call OpusCatHelpController visibility/open methods directly with construction order, optional chaining, open results, and focus return preserved. The four configured-provider, LM Studio, and OPUS-CAT help forwarding functions are removed, and release verification prevents them from returning.
- AiSegmentContextService now also owns direct-OpenAI explicit-settings resource-context composition: concurrent TM/termbase lookup start, stable `[tmMatches, terms]` result order, project language and selected-resource query construction, independent opt-outs, and rejection propagation. AiOpenAiSuggestionController consumes the public method directly, preserving unsaved global-form setting precedence and the TM query without an added result limit; `aiContextForSegment` is removed from `app.js`, and release verification prevents this orchestration from returning.
- A checked AiLocalSettingsPersistenceController now owns command-time local/hosted AI settings sequencing: no-project form fallback, non-throwing unsaved endpoint rejection, local settings store write, normalized merge with existing project AI settings, repository persistence, selected-project and project-list synchronization, workspace dirtiness, and silent versus visible saved status. Form composition/normalization and endpoint policy, the local settings store, project repository, EditorSessionStore records, provider/runtime selection, AI commands, workspace state, and status state remain injected boundaries. All ten local/hosted command controllers consume the silent method, and release verification rejects the former coordinator helper and sequencing returning.
- A checked AiCommandLifecycleCoordinator now owns lifecycle adapters for the seven batch-capable AI command controllers plus uniquely owned prompt-only adapters for project-brief generation and prompt testing: unconditional versus defined progress mirroring, prompt-busy ownership where applicable, owner-specific AbortController capture, latest-active-controller replacement, identity-guarded running/abort clearing, exact ordered cancel routing, and the legacy fallback abort/canceled-progress/presentation/status path. Each command controller retains its own running, progress, AbortController, provider, command, persistence, and status behavior; shared application-state mutation, progress presentation, and status storage remain injected boundaries. Release verification rejects the former private abort variables, prompt-busy flags, repeated sync blocks, shared cancel façade, and prompt-only synchronization blocks returning.
- Provider-form and provider-administration prompt-preview consumers now use exact late-bound adapters to the checked AiPromptPreviewController, while prompt testing, editor selection refresh, and workflow characterization call its public methods directly. The seven former `localAi*` prompt-preview forwarding functions are removed, omitted-argument defaults and startup order remain unchanged, and release verification prevents the compatibility façade from returning.
- All 24 AiAdministrationController action entries now late-bind directly to their checked settings, provider-administration, prompt-test, local/hosted command, and direct OpenAI owners; the two pretranslation entries intentionally share the same controller method and the single action-error boundary is unchanged. The command palette, OPUS-CAT retry, and 41 workflow-characterization call sites use public controller methods directly. Twenty-three unique forwarding functions and three dead pre-extraction review-selection helpers are removed, while release verification preserves labels, enablement safeguards, active/batch distinctions, and construction order.
- A checked ReviewStateController now owns the compact quick review-state toggle/clear lifecycle: ChangeReviewState command sequencing, repository persistence, review-form synchronization, optional activity logging, row/workspace/status updates, exact primary-failure rollback, and command restoration. EditorSessionStore records, CommandBus implementation, domain mutation/history preparation, repository, activity, presentation, workspace, status, and localized labels remain injected boundaries; full review-metadata form saves, quality decisions, confirmation, structural split/merge, and batch changes remain separate.
- A checked ReviewMetadataController now owns full review-form normalization, optional stable comment creation, segment revision touching, pending-save clearing, repository persistence, best-effort activity logging, forced review/row/history presentation, workspace/status updates, and exact primary-failure rollback. EditorSessionStore records, domain history preparation, AutosaveService, repository, activity, presentation, workspace, status, IDs, and timestamps remain injected boundaries; quick review commands, quality decisions, confirmation, structural edits, target replacement, and batch changes remain separate.
- A checked QualityDecisionController now owns decision category/severity normalization, localized structured-comment creation, the `needs-review` transition, segment revision touching, pending-save clearing, repository persistence, quality-risk queue replacement, decision-note clearing, review/workbench/row presentation, workspace/status updates, activity-warning results, and exact primary-failure rollback. EditorSessionStore records, domain history preparation, AutosaveService, repository, risk construction, activity, presentation, workspace, status, labels, IDs, and timestamps remain injected boundaries; quick/full review metadata, quality profiles, confirmation, structural edits, target replacement, and batch changes remain separate.
- A checked QualityProfileController now owns profile normalization, selected-project and project-list snapshots, project repository sequencing, selected-record/list synchronization, quality-risk replacement, project-summary refresh, workbench rendering, workspace dirtiness, optional activity-warning status, and exact project/list rollback on primary failure. EditorSessionStore records, profile/risk domain functions, repository, summary refresh, activity, presentation, workspace, and status remain injected boundaries; quick/full review metadata, quality decisions, confirmation, structural edits, target replacement, and batch changes remain separate.
- A checked StructuralSegmentController now owns split/merge eligibility, protected-range and target-to-source cursor mapping, pending-save flushes, stable structural IDs and contiguous ordering, typed command construction/execution, protected-tag regeneration, atomic structural save/delete, selection, rendering/focus, workspace/status updates, exact first-transaction rollback, and Undo/Redo restoration. EditorSessionStore records, CommandBus implementations, domain mutation/history preparation, AutosaveService, the atomic repository boundary, rendering, selection, status, and workspace dirtiness remain injected; ordinary typing, confirmation, target replacement, and batch pretranslation remain separate.
- Structural command-palette and workflow consumers now call StructuralSegmentController directly for protected ranges, cursor mapping, split/merge eligibility, next-segment selection, split/merge execution, and controller-owned Undo/Redo restoration. Seven forwarding functions plus two dead restoration wrappers are removed with current-segment defaults, booleans, promises/results, structure safeguards, focus/selection, persistence order, and retryable Undo/Redo preserved; release verification prevents all nine from returning.
- The 6,408-line application workflow characterization driver now lives under `tests/app-workflow/` and is composed into `app.js` lexical scope only by the test renderer build. Source and artifact checks reject the driver from the production graph while the existing deep browser workflow remains unchanged.
- Checked JavaScript/JSDoc, ESLint, Prettier, Stylelint/token enforcement, import-boundary checks, Node focused tests, axe checks, and visual regression checks. Prettier now follows the repository's `.gitattributes` LF contract, so the formatting gate is reproducible on a clean checkout.
- Explicit app store, navigation, platform, repository, preference, status/job/error, feature-controller, command, palette, theme, layout, update, diagnostics, safe-DOM, and report-template boundaries under `src/`.
- A quiet semantic design system with restrained surfaces, no persistent blur, semantic light/dark roles, Balanced/Compact density, remembered inspector/layout preferences, and responsive Projects → dashboard → editor layouts.
- Contextual inspector tabs, fuzzy/grouped `Ctrl/Cmd+K` palette, recent commands, filter presets, contextual AI suggestions, separated provider administration, inspectable provenance, AI apply-and-next, and reversible AI application.
- IndexedDB schema 6 with persistent manual Trash for projects, project files, individual TM entries, individual terms, whole translation memories, and whole termbases while preserving project-package schema 5 and compatibility with existing package/backup paths.
- Transactional project/file Trash, segment confirmation/confirm-and-next, quick review-state, target replacement, segment splitting/merging, and AI-application operations with Undo/Redo. Command restores use monotonic revisions so IndexedDB's stale-write protection cannot silently reject an Undo.
- Resource deletion now uses the same bounded command/Undo/Redo model instead of immediate confirmation and hard deletion. Resource records and their token indexes are removed atomically; restoration recreates exact content and metadata, marks indexes for safe lazy rebuild, refreshes linked projects, and blocks ID or same-name/language-pair conflicts without overwriting live data or discarding the Trash item. Empty Trash remains the only irreversible deletion action.
- `SplitSegment` and `MergeSegment` use independent typed structural commands over one atomic IndexedDB boundary. Merge updates the surviving segment and deletes the merged-away segment in one transaction; Undo restores both stable IDs/order/history, Redo deletes only the merged-away segment with monotonic revisions, and a forced first-transaction failure records no command or partial state.
- Ordinary target typing now uses coalesced `EditTarget` commands: one continuous typing session creates one redacted Undo receipt while preserving the 450 ms autosave/retry path, exact target history/provenance restoration, persisted Undo/Redo, active selection, and monotonic revisions.
- Copy Source, TM-match/concordance insertion, and protected-tag insertion now use discrete redacted commands. A pending typing session is finalized below the producer command, and Undo/Redo restores target state, persistence, selection, provenance boundaries, monotonic revisions, and protected-tag caret placement.
- TM and Local/Hosted AI pretranslation now use atomic, bounded batch commands. TM applies all matched patches in one storage transaction; AI persists successful provider results only after completion, rolls back all in-memory output on cancellation, preserves confirmed/locked safeguards, and exposes one redacted Undo/Redo operation with TM/AI provenance and review-state restoration.
- Actionable service-worker update lifecycle that waits for user action and flushes pending saves before activation/reload.
- Centralized trusted DOM/report boundaries, CSP Trusted Types enforcement in Electron/Chromium, safe worker/service-worker URL construction, and no remaining unallowlisted raw `innerHTML` writes in application code.
- Local-only redacted diagnostics covering runtime, sandbox/GPU status, storage estimates, update state, and normalized errors without source, target, prompt, key, or exact-path content.
- Translator-first modernization copy in English, Catalan, and Turkish. The locale catalogs validate with no blank Catalan or Turkish messages.
- The downloadable web package now selects a self-contained `file://` renderer entry while HTTP(S) retains lazy ES modules; direct-file and hosted smoke tests exercise real New project and About interactions, and Electron keeps its static private-protocol entry.

## Measured results

- Packaged production JavaScript graph: 2,586,370 bytes across five modules, including both mutually exclusive startup paths.
- Hosted/desktop initial production `app.js`: 1,048,614 bytes minified and 283,873 bytes gzip. Its two locale chunks remain lazy.
- Direct-file web fallback `app-file.js`: 1,293,163 bytes minified and 366,954 bytes gzip; it is self-contained because browsers block module imports from `file://` and is not executed by HTTP(S) or Electron.
- Initial synchronous JavaScript is more than 40% below the recorded approximately 2.62 MB baseline. Hosted gzip remains within the 250 KiB-class budget but is 33,873 bytes above a strict 250,000-byte interpretation; the hosted entry remains 298,614 bytes above the 750 KB minified stretch target.
- Visual verification passes 81 deterministic screenshots covering 1440×900, 1366×768, and 1024×768, including an actionable import-validation failure, visible focus recovery, the local recovery panel and open workspace-status menu, populated Comments and Quality Workbench inspectors, Resources TM dashboard/detail, termbase dashboard, populated resource Trash, post-restore empty Trash, the TM threshold and OPUS-CAT help dialogs, provider administration/AI Command Centre, light/dark, inspector open/closed, Focus mode, AI, status, and compact-density states.
- Automated accessibility checks pass the deterministic Projects, import-validation error and focus-return path, actionable local recovery panel, open workspace-status menu, Resources translation-memory/termbase empty states, populated resource Trash, populated Comments and Quality Workbench states, AI provider administration/Command Centre and focus-return path, New project dialog, About dialog, TM threshold dialog, OPUS-CAT help dialog, and command-palette states with zero blocking findings. This is not a WCAG conformance claim.
- The full Electron browser suite passes security, offline shell, smoke, regression, application workflow, workspace storage, package round trip, and large-project coverage.
- The packaged Windows desktop passes artifact inspection, fuse verification, renderer OS sandbox verification, GPU-enabled startup, and the explicit GPU-disabled fallback.

## Partial roadmap packages

### P1-08 — Remaining `app.js` extraction

The new checked boundaries are active, but `app.js` remains the compatibility coordinator and is still 9,962 source lines. The 6,408-line workflow driver is external and composed only into the test graph, but the roadmap's source goal of a bootstrap under 300 lines is not met.

The synchronous dialog-lifecycle, async project-dialog, TM-threshold, OPUS-CAT help, Resources, quality/review, recovery/workspace, import/export/report, AI provider-administration/command-centre, target-edit, autosave, segment-confirmation, discrete target-producer, target-replacement, TM and local/hosted AI batch-pretranslation, active/batch AI review, protected-tag repair, target alternatives, terminology application/extraction, draft polish/adaptation, project-brief generation, direct OpenAI suggestion requests, shared AI suggestion persistence, project AI-settings persistence, local-provider connection/model administration, AI suggestion application, quick review-state, full review-metadata, quality-decision, quality-profile, and structural split/merge slices are complete. All 18 AI registry positions now install through checked adapters rather than provider implementations in `ai.js`. This includes the hosted OpenAI-compatible family (Groq, Together AI, OpenRouter, Hugging Face Inference Providers, DeepInfra, and Fireworks AI), the native Responses family (OpenAI, xAI, and Azure OpenAI), native chat-completion providers (DeepSeek and Mistral), Perplexity Sonar, Google Gemini Interactions, Anthropic Messages, Cohere Chat V2, local/hosted Ollama, loopback/allowlisted OpenAI-compatible servers, and OPUS-CAT direct/bridge MTRestService. Their original registry positions, endpoints, credentials, payloads, parsing, metadata, timeout/cancellation behavior, redacted errors, consent rules, and temporary compatibility exports are characterized independently. Release verification rejects any provider object implementation or provider registration returning to `ai.js`, and now also rejects target textarea listeners, pending autosave timer state, segment-confirmation event/busy state, discrete target-producer command/event orchestration, target-replacement input/event/mutation/command/persistence/activity/presentation/rollback orchestration, TM-pretranslation busy/candidate/lookup-loop/mutation/command/persistence/activity/presentation/rollback orchestration, AI-pretranslation validation/consent/lifecycle/provider/mutation/command/persistence/activity/presentation/rollback orchestration, AI-review validation/consent/lifecycle/selection/risk/comment/mutation/persistence/activity/presentation/recovery orchestration, AI-tag-repair validation/consent/lifecycle/selection/protected-token/suggestion/persistence/activity/presentation/recovery orchestration, AI-alternatives validation/consent/lifecycle/selection/context/variant/suggestion/persistence/activity/presentation/recovery orchestration, AI-terminology-application validation/consent/lifecycle/selection/context/suggestion/persistence/activity/presentation/recovery orchestration, AI-draft-editing validation/consent/lifecycle/selection/context/mode/suggestion/persistence/activity/presentation/recovery orchestration, AI-terminology-extraction validation/consent/lifecycle/selection/provider/term-persistence/activity/presentation/status orchestration, AI-project-brief validation/consent/lifecycle/context/provider/project-persistence/administration/activity/presentation/status/recovery orchestration, AI-suggestion-application validation/command/restoration/mutation/persistence/activity/presentation/navigation/status/recovery orchestration, AI-suggestion normalization/append/persistence/activity/presentation/workspace/status/recovery orchestration, AI-settings form/normalization/endpoint/snapshot/project/list/key/activity/presentation/workspace/status/recovery orchestration, local-provider connection/model validation/provider/desktop-retry/discovery/model-state/progress/status orchestration, direct-OpenAI validation/consent/setup/key/context/provider/suggestion-storage-result/presentation/workspace/status/recovery orchestration, quick review-state command/mutation orchestration, full review-metadata mutation/persistence/activity/presentation/rollback orchestration, quality-decision normalization/mutation/persistence/risk/activity/presentation/rollback orchestration, quality-profile normalization/snapshot/persistence/risk/summary/activity/presentation/status/rollback orchestration, or structural mapping/command/restoration/button ownership returning to `app.js`. Still required before this package is complete:

Checked replacement/autosave/confirmation/TM-pretranslation consumer wiring, remaining target-edit/autosave application and workflow direct wiring, structural command/workflow/restoration direct wiring, target-edit selection and discrete target-producer consumer wiring, AI prompt preview/testing and direct consumer wiring, direct AI command dispatch, AI terminology-candidate persistence, AI suggestion-list presentation, shared AI segment context including direct-OpenAI explicit-settings resource composition, shared AI scope/sample selection, external-send consent, provider presentation, browser credential storage, runtime-settings composition/readiness, provider-form/command-centre orchestration, command-time local-settings persistence, shared batch-command lifecycle/cancellation, prompt-only project-brief/prompt-test lifecycle adapters, suggestion/context consumer direct wiring, and provider/help consumer direct wiring are also complete: release verification now rejects checked editor-controller, target-edit/autosave, structural, and target selection/producer forwarding functions, prompt mode/context/request construction, prompt-test validation/consent/provider/lifecycle/presentation orchestration, prompt-preview or command-dispatch forwarding functions, obsolete review selectors, term-candidate lookup/deduplication/record/save-loop/workspace orchestration, suggestion-list empty/card/provenance/inspection/explanation DOM and apply-intent routing, termbase/TM eligibility/lookup/warning/nearby-segment selection or direct-OpenAI resource composition, terminology/pretranslation scope and project-brief sample policy, disclosure normalization/list grammar/question/confirmation policy, provider privacy/endpoint/pull/capability/badge/summary policy, credential storage acquisition/read/write/clear/scope/precedence/snapshot/rollback/label policy, project-setting normalization/form-composition/endpoint/runtime-key/readiness policy, provider/preset/model/command-centre view-model and form-transition sequencing, local-store/project/list/workspace/status persistence sequencing, batch lifecycle-state/AbortController identity/ordered-cancel/fallback orchestration, prompt-only private prompt-busy ownership, or the removed suggestion/context/provider/help forwarding functions returning to the compatibility façade.

1. Reduce the remaining `app.js` compatibility coordinator toward the roadmap's bootstrap-only goal, one characterized feature boundary at a time.
2. Keep each extraction behavior-preserving and run the focused feature suite plus the full browser/release suite at each family boundary.

### P2-05 — Performance stretch target

Lazy locale chunks, production/test graph separation, minification, update lifecycle, and offline asset generation are delivered. The hosted/desktop initial bundle meets the relative-reduction and 250 KiB-class gzip targets but remains 298,614 bytes above the 750 KB minified stretch target. The separately loaded direct-file fallback is a compatibility artifact and must be tracked independently. Further reduction should come from the P1-08 feature extractions and lazy loading of the remaining uncommon feature families, not from removing offline capability or mature format support.

## Manual and external release gates still required

- NVDA/Windows and VoiceOver/macOS audited-flow checks, including keyboard, focus return, 200% zoom, reduced motion, increased contrast, and forced colors.
- Signed packaged smoke/artifact evidence on macOS and Linux in addition to the completed Windows evidence.
- Cold/warm startup, typing latency, long-task, and large-project scroll measurements on the named reference laptop. The roadmap targets remain targets until this evidence is recorded.
- Final release-candidate visual review by a human at all three required viewports and both themes.

## Current green automated gates

- `pnpm verify:release`
- `pnpm verify:desktop-wrapper`
- `pnpm verify:ai-sidebar-ux`
- `pnpm i18n:validate`
- `pnpm verify:web-artifact`
- `pnpm verify:web-smoke`
- `pnpm verify:browser-runner`
- `pnpm verify:quality`
- `pnpm verify:a11y`
- `pnpm verify:visual`
- `pnpm run pack`
- `pnpm verify:artifact`
- `pnpm verify:fuses`
- `pnpm verify:desktop-smoke`

## Recommended next implementation task

Continue P1-08 with a behavior-preserving editor-context and quality/review consumer-façade cleanup. EditorContextController already owns contextual sidebar refresh sequencing, while QualityReviewController and the checked quality profile, quality decision, review metadata, and quick review-state controllers own their respective render, form-read, persistence, command, rollback, and status behavior. `app.js` still retains six forwarding functions around those public methods. Route application and workflow consumers directly, retaining exact call-time adapters only where the receiving controller is composed before its dependency is assigned.

Entry criteria:

- Characterize EditorContextController refresh order, superseded-refresh results, empty-context behavior, and rejection propagation; preserve every awaited and fire-and-forget call site.
- Preserve review rendering's current-segment lookup, optional `force` normalization, optional-controller behavior, and return behavior while replacing `renderReviewPanel`.
- Preserve quality profile, quality decision, and full review-metadata default form reads, explicit-value forwarding, promise results, and rejection behavior; preserve quick review-state command results and failures.
- Preserve late binding where AI/editor-context/QualityReviewController composition precedes `editorContextController`, `qualityReviewController`, or the checked quality/review mutation controllers; use direct public methods after initialization.
- Preserve the completed AppStore characterization across Projects → dashboard → editor, project/file switching, Focus mode, filters, Undo/Redo, and reload recovery while project/session ownership moves.
- Preserve every extracted AI provider, administration, settings, credential, consent, prompt, command, suggestion, context, scope, lifecycle/cancellation, presentation, and offline boundary.
- Remove only `refreshSidebar`, `renderReviewPanel`, `saveQualityProfileFromForm`, `saveQualityDecisionFromForm`, `saveActiveReviewMetadata`, and `setActiveReviewState`; keep context selection, rendering, form reads, quality/review mutations, persistence, commands, rollback, activity, presentation, workspace, and status behavior in the existing checked owners.
- Do not combine state ownership changes with visual redesign, storage migration, provider changes, or new features.

Exit criteria:

- Application, controller, and workflow consumers call EditorContextController and the checked quality/review controllers directly or through exact construction-order adapters; the six forwarding functions are removed.
- Release verification prevents the editor-context/quality-review compatibility façade from returning, and focused editor-context/quality/review/command tests plus all full release gates pass with no intended behavior change.
- Focused controller/repository tests, provider adapter tests, security policy, AI UX, web/desktop smoke, app workflow, package/workspace round trips, and FULL-SUITE gates pass with no intended user-visible difference.

Continue until `app.js` is a bootstrap-only compatibility entry and the façade can be removed without changing mature LoopCAT behavior.
