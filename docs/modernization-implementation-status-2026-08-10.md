# LoopCAT modernization implementation status

Status date: 2026-08-20
Baseline commit: `0a2d5343d6c2a2ef859a16bc0674e128e7803d1a`
Implementation state: substantial P0-P3 delivery, with target editing, autosave, their direct application/workflow consumers, editor-context and quality/review direct consumers, EditorSessionStore direct getter consumers, AppStore/FilterStore and ApplicationNavigation direct consumers, application-menu, application-view, command-button, update-control, and segment-grid scroll-listener lifecycle, checked UI/report-localization, language-input and text-encoding input policy, portable-file input and shared import-task lifecycle policy, protected-tag inspection and protected-text replacement policy, segment AI/TM provenance classification/presentation, segment filter predicate/index/cache policy, segment source-word/progress derivation and cache sequencing, segment target-history/patch/revision state policy, segment command-restoration orchestration, segment confirmation-state mutation/recovery policy, direct segment-TM-save orchestration, concordance search/presentation/lifecycle orchestration, active-segment selection/next-open navigation orchestration, segment-draft application policy, command-palette direct-consumer wiring, target-replacement panel lifecycle, application-global keyboard routing, quality-presentation and AI-review risk-label policy, quality-workbench risk-queue/evidence/refresh/navigation orchestration, revision-history label/safe-presentation policy, project-language context/spellcheck orchestration, project document-catalog/selection, document-statistics, project-document import, project-package portability/preparation, project-package import/browser-backup restore, workspace connection/project-package save/autosave, workspace-folder sync, workspace backup-export, and workspace health/manifest-repair orchestration policy, and project-dialog create/edit save orchestration, report-presentation, report-document composition, report-data collection, report-export, delivery-export, current-project resource-transfer, project resource selection and language-pair shortcuts, resource catalog, resource-library import/export, Resources presentation, resource mutation orchestration, term-suggestion presentation/deletion, term-form save/lifecycle orchestration, current-project QA execution/activity/lifecycle, and project-domain save/rollback/form-lifecycle policy and direct consumers, segment confirmation, discrete target producers and direct consumers, target replacement and checked editor-controller direct consumers, TM and local/hosted AI batch pretranslation, active/batch AI review, protected-tag repair, target alternatives, terminology application/extraction and candidate persistence, draft polish/adaptation, project-brief generation, direct OpenAI suggestion requests and resource-context composition, shared AI suggestion persistence, suggestion-list presentation, segment context, scope/sample selection, external-send consent, provider presentation, browser credential storage, runtime-settings composition/readiness, provider-form/command-centre orchestration, command-time local-settings persistence, shared AI command lifecycle/cancellation, prompt-only AI lifecycle adapters, prompt-preview compatibility-façade cleanup, AI command-dispatch direct wiring, suggestion/context consumer-façade cleanup, and provider/help consumer-façade cleanup, project AI-settings persistence, local-provider connection/model administration, AI prompt preview/testing, AI suggestion application, quick/full review metadata, quality decisions, quality profiles, and structural split/merge orchestration and direct consumers extracted and the remaining P1-08 coordinator work plus the performance stretch target still partial

The latest completed P1-08 checkpoints also move QA-results aggregation/filter/safe-presentation/navigation, active-segment TM-match lookup/stale-result/presentation/insertion, term-suggestion lookup/stale-result/presentation/deletion, term-form save/lifecycle, current-project QA execution/activity/lifecycle, project-domain save/rollback/form lifecycle orchestration, AI-review risk-label presentation, project-document import routing/persistence/navigation/presentation, project-package validation/structure/clone/copy-name/ID-remapping/preparation policy, portable-file validation/decoding/progress plus shared import-task lifecycle, project-package/browser-backup construction plus write-validation policy, manual project-package/browser-backup export action orchestration, project-package import/browser-backup restore orchestration, workspace connection/project-package save/background-autosave/recovery/timer/settings orchestration, project-dialog create/edit validation/persistence/activity/workspace-save orchestration, workspace-folder package iteration/import/warning/reset/presentation orchestration, workspace full-backup build/write/status/validation/failure orchestration, workspace manifest-repair/health-report/presentation/status orchestration, shared application-menu selection/close/listener lifecycle, segment-grid viewport scroll-listener/coalescing lifecycle, application-view routing/render/refresh plus primary-navigation listener lifecycle, optional Empty Trash/Undo/Redo listener lifecycle, and late-bound Reload/Defer update-control lifecycle behind checked controller or service boundaries.

The latest UI-locale controls checkpoint also moves locale-change sequencing plus select/import/export listener lifecycle behind a checked controller boundary while leaving catalog parsing, storage, export construction, and full localized-UI refresh policy in their existing application services.

The latest project-home checkpoint also moves current-project navigation, active-index selection, Project files listener lifecycle, and no-argument Project home Delete delegation behind a checked controller boundary while leaving project deletion and Trash orchestration in their existing services.

The latest Focus-mode checkpoint also moves Focus presentation, project-aware state transitions, menu closing, deferred segment rendering/target focus, and toggle/exit listener lifecycle behind a checked controller boundary while retaining the existing application store, editor renderer, and target editor as injected owners.

The latest inspector-toggle checkpoint also moves live inspector-state inversion, non-awaited layout persistence, immediate editor rendering, and open/close focus routing behind a checked controller boundary while retaining inspector-tab policy and workspace-layout preference handling in their existing owners.

The latest command-palette trigger checkpoint also moves the optional toolbar-trigger listener lifecycle behind the existing immutable PaletteController while leaving its asynchronous initialization, command search/execution, recent-command persistence, localization, overlay, and focus policy unchanged.

The latest project-filter controls checkpoint also moves the project-search and language-pair listener lifecycle plus the filtered-empty-state clear/render/focus sequence behind a checked controller boundary while leaving project lookup, filtering, sorting, option construction, tiles, and the no-project import action unchanged.

This document records what the current working tree implements from the 2026 modernization roadmap. It supplements, and does not replace, the review and plan. The repository must not be described as having completed the entire roadmap until the partial items below and the remaining manual/platform gates are closed.

## Delivered

- Deterministic characterization fixtures, bundle/version-contract enforcement, three-viewport screenshot capture, accessibility automation, and focused unit/static gates.
- Separate esbuild production/test renderer graphs. Production verification rejects workflow harnesses, mocks, fixtures, debug routes, source test globals, and test imports.
- One generated production asset manifest used by renderer staging, web packaging, desktop packaging/protocol allowlisting, service-worker caching, and artifact verification.
- Electron 43.3.0, electron-builder 26.15.3, Node 24 tooling, renderer sandboxing, context isolation, secure fuses, hardware acceleration by default, and an explicit restart-based GPU fallback.
- Immediate accessibility repairs for live status, focus visibility, modal focus containment/restoration, disclosure state, and command-palette keyboard behavior.
- A checked shared dialog-lifecycle controller now owns open, close, native cancel, initial focus, and explicit focus return for About, Diagnostics, Trash, the TM pretranslation threshold prompt, and OPUS-CAT connection help. Feature-local adapters return only user intent: TM matching and provider/network ownership remain in their existing services. TM cancel returns to the visible Segment tools trigger; OPUS-CAT help returns to its visible Project settings entry point. The superseded `showManagedDialog` helper and direct TM/OPUS lifecycle listeners are removed.
- A checked project-dialog controller owns create/edit mode, asynchronous resource preparation, field and delegated dynamic listeners, workspace-folder interaction, AI-settings deep linking, and lifecycle delegation. The existing domain save service remains the only persistence/validation boundary, and no project/resource/workspace data moved into UI state.
- A checked Resources controller now owns Resources navigation, TM/termbase tab state and keyboard behavior, view-local resource selection, language-field normalization, import-input lifecycle, card/detail/row event delegation, initial focus, and focus restoration across asynchronous resource refreshes. Existing parsers, storage, exports, validation, linked-project dirtiness, and recovery boundaries remain injected domain services. Empty Resources actions now open the dedicated resource import inputs instead of the project-level import inputs.
- A checked quality/review controller now owns Comments and Quality Workbench rendering, review/profile/decision form values and event lifecycles, delegated risk navigation, in-progress form preservation, deterministic empty states, and focus restoration across risk-list rendering. Quality scoring, QA, persistence, commands, status/activity, workspace dirtiness, AI review safeguards, and Quality Passport/report generation remain injected domain boundaries.
- A checked recovery/workspace controller now owns workspace health and local-storage presentation, connected/unsupported/busy control states, project-folder status, recovery and backup-reminder rendering, workspace action listeners, recovery dismissal, menu access, cleanup, and visible focus restoration when recovery UI disappears. Directory handles, workspace manifest v1, project-package schema 5, database/backup schema 6, dirty-marker persistence, pending-save flushes, autosave, package validation/import conflict policy, repair, redaction, jobs, and status remain injected domain boundaries. The recovery-folder action now stops the application outside-click handler from immediately closing the menu.
- A checked import/export controller now owns project-file, project-package, browser-backup, TMX, TBX, term-list, delivery, project-report, Quality Passport, and anonymized-report event lifecycles; sequential multi-file input reset; one shared import busy state; safe validation-report DOM; delegated dismissal; auto-dismiss timing; and visible focus restoration. Parsers, encoding detection, format reconstruction, package/backup replacement, pending-save flushes, report generation, downloads, validation/redaction policy, activity history, workspace dirtiness, storage, and status remain injected application boundaries.
- A checked AI administration controller now owns global and project provider-form values, provider preset/model options, safe provider-summary DOM, privacy/status/progress/busy presentation, prompt previews, output disclosure, static contextual/command-centre action listeners, language-field event lifecycle, and cleanup. Provider registries/adapters, endpoint allowlists, credential storage, prompt construction, external-send consent, batches/cancellation, suggestions, commands/Undo, provenance, project persistence, activity history, redaction, and status/error decisions remain injected application boundaries.
- A checked immutable compatibility-module registry now makes the runtime installer the single bridge from the legacy `window.CatHan` module namespace into the application runtime. `app.js` reads only `window.CatHan.appRuntime`; storage, projects, AI, encoding, format handlers, workspace storage, i18n, focus, workers, QA, validation, analysis, and quality APIs are injected through the runtime. Release verification rejects direct feature-global access returning to the coordinator.
- AppStore is now the sole writer for application route, selected project/document identity, active segment identity, and Focus mode. Editor filters are owned by one checked FilterStore, while a checked EditorSessionStore owns project/session record replacement through explicit selectors and actions; the temporary compatibility bridge has been removed.
- The project-summary cache and its dirty-revision map are the first EditorSessionStore family removed from that bridge. Dashboard reads, replacements, copy-on-write dirty increments, stale-revision pruning, and workflow fixtures now use explicit checked store APIs; release verification rejects either legacy `state` property returning.
- Project-term and activity-event contextual caches are also removed from the EditorSessionStore compatibility bridge. Project switching and terminology refresh use explicit checked replacement APIs, while activity logging uses a newest-first deduplicated prepend action; reports, backup reminders, workspace persistence, and workflow characterization read through checked selectors.
- QA-check and quality-risk contextual caches are now removed from the EditorSessionStore compatibility bridge. Successful QA and report generation use explicit checked replacement actions, failed QA preserves the previous checks, and Quality Workbench rendering, risk navigation, profile/decision refreshes, reports, and workflow characterization read through checked selectors.
- The derived project progress-summary cache is now removed from the EditorSessionStore compatibility bridge. Progress rendering reads and replaces the cache through checked APIs while preserving its project/segment-count validity guard, incremental confirmed-status delta, single-pass fallback, cached source word counts, and existing UI output.
- The core project collection and current-project record are now removed from the EditorSessionStore compatibility bridge. Loading, creation, switching, settings edits and rollback, imports, project-package history, Trash/Undo, recovery, workspace sync, reports, AI context, and workflow fixtures use explicit checked selectors and replacement actions.
- The segment collection is the final family removed from the EditorSessionStore compatibility bridge. Loading, imports, project/package restore, commands and Undo/Redo, autosave/retry, pretranslation, QA/review, document filtering, virtualization, structural split/merge, and workflow fixtures read through the checked selector; whole-collection and indexed replacements use explicit actions, with indexed replacement preserving copy-on-write identity. The temporary `attachCompatibility` API is deleted and release verification prevents it or direct legacy `state.segments` access from returning.
- A checked SegmentGridController now owns the virtual window, coalesced scroll and row-update frames, idempotent viewport scroll-listener lifecycle, active-segment dispatch, visibility scrolling, and focused target-editor lookup. A checked EditorContextController coordinates Matches, Terms, Review, History, AI suggestions, and Quality refreshes while their domain services and renderers remain injected.
- A checked TargetEditController now owns target-textarea focus/blur, input/composition, keyboard Undo/Redo and confirmation/navigation routing, caret lookup/normalization/restoration, and EditTarget session coalescing. A checked AutosaveService owns the 450 ms pending-save queue, latest-session-record selection, 2 s retry, project/document clearing, forced flush recovery, and repository sequencing. EditorSessionStore, CommandBus, repository, status, rendering, workspace dirtiness, and domain-record mutations remain injected boundaries; discrete source/match/tag producers, structural split/merge, and bulk changes remain separate.
- A checked SegmentConfirmationController now owns the Confirm button lifecycle, per-segment busy state, protected-tag blocking, CommandBus sequencing, confirm-and-next navigation, filter/progress/history rendering decisions, primary failure recovery, persisted monotonic rollback, and non-transactional TM/activity warning results. EditorSessionStore records, domain mutations, repository writes, TM/activity services, status, workspace dirtiness, and command restoration remain injected boundaries; ordinary target editing, discrete source/match/tag producers, structural split/merge, review-state changes, and batch pretranslation remain separate.
- A checked TargetProducerController now owns the Copy Source button lifecycle and shared Copy Source, TM match/concordance insertion, and protected-tag insertion orchestration: pending EditTarget finalization, patch/caret capture, redacted command execution, provenance, filter-sensitive rendering, autosave scheduling, workspace dirtiness, success status, and exact failure rollback. EditorSessionStore records, CommandBus implementations, domain mutations, AutosaveService, command restoration, rendering, status, and selection remain injected boundaries; ordinary typing, confirmation, structural split/merge, review-state changes, and batch pretranslation remain separate.
- Target restoration now calls TargetEditController selection normalization directly, while command-palette, TM-match, concordance, protected-tag UI, and workflow consumers call TargetProducerController public methods directly. The four target selection/producer forwarding functions are removed with omitted options, promise/results, provenance channels, caret placement, failure containment, and Undo/Redo behavior preserved; release verification prevents them from returning.
- A checked ConcordanceController now owns selected-keyword precedence and whitespace normalization, escaped case-insensitive highlighting, editor/project eligibility, linked-TM and language-pair filtering, newest-first result ordering, localized result metadata and empty state, safe card/button construction, target-insertion intent, overlay open/close/result clearing, and close-button/backdrop lifecycle. EditorSessionStore, navigation state, TM repository and resource selectors, language context, localization, safe HTML, TargetProducerController, status, and browser DOM primitives remain injected boundaries; command-palette and global-key routing remain in the compatibility coordinator. Four concordance helpers and two app-owned listeners are removed, while palette, shortcut, Escape, and target-producer consumers call the immutable controller directly.
- A checked SegmentNavigationController now owns active-index bounds/idempotence, grid and inspector selection, confirmation-busy refresh, filtered-position visibility, old/new row refresh, AI prompt-preview refresh, awaited contextual-inspector refresh, next-open after-current/wraparound selection, conditional status-filter reset/rerender, and target focus. EditorSessionStore, AppStore navigation, SegmentGridController, inspector, confirmation, filter/store policy, presentation, context, focus, and the status-filter element remain injected boundaries. Three early-composed controllers retain exact late-bound adapters; fifteen later application consumers and all twenty-four workflow consumers call the immutable controller directly, and the three navigation helpers are removed.
- A checked SegmentDraftApplicationService now owns TargetEditController's synchronous mutation seam: prior-status fallback, pre/post filter membership, target/status/history mutation, conditional filter invalidation, preserve-scroll rerender versus scheduled/canceled row updates, progress delta, deferred history rendering, workspace dirtiness, and post-mutation patch return. SegmentTargetStateService, SegmentFilterService, SegmentGridController, presentation, and workspace effects remain injected boundaries; target textarea events/composition, EditTarget coalescing, autosave, and navigation remain separate. TargetEditController calls the immutable service directly and the local `applyTargetDraft` helper is removed.
- Global-key, toolbar, and workflow command-palette consumers now call PaletteController's optional public methods directly. The two global shortcut qualifications, cancellation/early-return behavior, Escape priority, toolbar behavior, focus containment/restoration, and startup/test degradation semantics remain unchanged; the unused render façade and the `openCommandPalette` and `closeCommandPalette` forwarding functions are removed, and release verification prevents all three façades or indirect consumers from returning.
- A checked immutable GlobalKeyboardController now owns capture-phase listener lifecycle and application-global routing for Undo/Redo, both command-palette shortcuts, Focus mode, concordance, and prioritized Escape handling. CommandBus capability/action methods, current project/view/focus state, overlay visibility, PaletteController, ConcordanceController, and Focus-mode actions remain injected boundaries; target-textarea keyboard behavior remains in TargetEditController. `wireEvents` mounts the controller at the former listener position, `handleGlobalKeydown` is removed, and release verification prevents listener or routing ownership from returning to the compatibility coordinator.
- A checked immutable QualityPresentationService now owns localized quality-profile, category, decision-severity, and risk-level mappings and fallbacks. UiLocalizationService and the optional base category label remain injected; report presentation/composition, QualityReviewController, and QualityDecisionController call the public methods directly. The four `qualityLabel`, `qualityCategoryName`, `qualityDecisionSeverityLabel`, and `qualityRiskLevelLabel` coordinator functions and their mapping literals are removed, while localization-catalog extraction scans the new source.
- A checked TargetReplacementController now owns replacement-panel open-before-focus behavior, the visible/project-wide replacement button lifecycle, input and filter-option reads, protected-token proposal/count construction, pending-save flush, ReplaceTargets command creation/execution, snapshot and Undo/Redo restoration coordination, atomic batch save, target history/status mutation, best-effort activity logging, tag-QA warning status, presentation refresh, focus recovery, and exact primary-failure rollback. EditorSessionStore records, filter indexes, protected-token replacement, CommandBus implementation, AutosaveService, repository, mutation/history adapters, command restoration, selection, presentation, activity, and status remain injected boundaries; ordinary typing/autosave, discrete target producers, confirmation, structural edits, quality/review saves, and batch pretranslation remain separate. The command-palette action calls the immutable controller directly and the local `openReplacePanel` helper is removed.
- A checked TmPretranslationController now owns the Pre-translate button lifecycle, threshold/re-entry/busy state, empty/confirmed/locked eligibility safeguards, deduplicated batched TM lookup with progress yielding, threshold proposal filtering, pending-save flush, patch/snapshot capture, redacted atomic command execution, normalized TM provenance with stale AI provenance clearing, batch persistence, best-effort activity/sidebar refreshes, workspace/status/presentation updates, exact primary-failure rollback, and unconditional busy cleanup. EditorSessionStore records, threshold dialog, TM lookup, CommandBus implementation, AutosaveService, repository, mutation/history adapters, command restoration, selection, presentation, activity, workspace, and status remain injected boundaries; manual target editing/replacement, local/hosted AI pretranslation, confirmation, structural edits, and quality/review saves remain separate.
- Target-replacement workflow, target-editor and discrete-producer autosave scheduling, active-segment confirmation-busy refresh, confirmation command-palette/workflow actions, and TM-pretranslation workflow consumers now call checked public methods directly. The TM-threshold dependency retains one exact late-bound adapter with its unavailable-browser error, visible Segment tools focus-return target, and promise/result preserved. The six replacement/autosave/confirmation/TM forwarding functions are removed, and release verification prevents them from returning.
- All remaining application, controller, and workflow target-edit/autosave consumers now call TargetEditController and AutosaveService public methods directly. The confirmation controller retains one exact call-time focus adapter because it is composed before TargetEditController assignment; all later consumers bind directly. Project-versus-global EditTarget finalization, pending-save clear options/defaults, inspection, flush results/rejections, draft-update returns, focus/caret results, retry scheduling, and Undo/Redo behavior remain unchanged. The nine `finalizePendingEditCommand`, `finalizePendingEditCommands`, `clearPendingSave`, `clearAllPendingSaves`, `pendingSaveRecords`, `clearPendingDocumentSaves`, `flushPendingSegmentSaves`, `updateSegmentDraft`, and `focusActiveTextarea` façades are removed, and release verification prevents them from returning.
- Application, controller, and workflow contextual-sidebar consumers now call EditorContextController directly after initialization or through the nine exact call-time dependencies required before its assignment. Review rendering likewise calls QualityReviewController with the current segment and normalized `force` option, while quality profile, quality decision, full review-metadata, and quick review-state consumers call their checked mutation controllers directly with controller-read form values. Refresh ordering, concurrent contextual services, stale-refresh detection, empty-context normalization, rejection propagation, optional review presentation, form defaults, command results, rollback, Undo/Redo, and status behavior remain unchanged. The six `refreshSidebar`, `renderReviewPanel`, `saveQualityProfileFromForm`, `saveQualityDecisionFromForm`, `saveActiveReviewMetadata`, and `setActiveReviewState` functions are removed, and release verification prevents them from returning.
- Application, controller, default-parameter, and workflow consumers now call the checked EditorSessionStore getters directly. Stable getter identities are passed where a controller expects a callback; application-specific selection and fallback policy remain at their existing call sites. Empty/default references, latest replacement identity, list iteration and ordering, active-record lookup, stale-summary and risk guards, copy-on-write updates, and derived-cache validity remain unchanged. The nine `currentProjects`, `currentProject`, `currentSegments`, `currentProjectSummaries`, `currentProjectTerms`, `currentActivityEvents`, `currentQaChecks`, `storedQualityRiskQueue`, and `currentProgressSummary` selector façades are removed, and release verification prevents them from returning.
- Application, controller, callback, and workflow consumers now read AppStore navigation/Focus-mode state and FilterStore state directly. The twelve injected active-index callbacks retain exact call-time field-selection closures; filter writes remain receiver method calls. Snapshot defaults and latest-reference identity, null-versus-empty-string navigation values, active-index `-1`, Focus-mode reset, filter object identity, atomic merge behavior, update returns, listener ordering, workflow access, and all navigation/filter-driven rendering remain unchanged. The eight `currentApplicationView`, `currentProjectId`, `currentDocumentId`, `currentActiveIndex`, `currentSegmentId`, `currentFocusMode`, `currentEditorFilters`, and `updateEditorFilters` consumer façades are removed, and release verification prevents them from returning.
- Application, controller, import, restoration, filter, and workflow consumers now call NavigationController directly. Derived segment selections retain exact call-time EditorSessionStore ID fallback; document selections retain optional-field omission and explicit-empty semantics; editor opening retains snapshot/override precedence; and render-time legacy navigation synchronization still precedes locale dispatch. Navigation returns, `SEGMENT_SELECTED` events, view/project/document/segment/index identity, Undo/Redo restoration, import selection, quality-risk navigation, and filter behavior remain unchanged. The four `selectApplicationSegment`, `selectApplicationDocument`, `applicationNavigationPayload`, and `syncLegacyApplicationState` compatibility helpers are removed, and release verification prevents them from returning.
- A checked UiLocalizationService now owns catalog-key and source-text delegation, missing-i18n fallbacks, locale fallback order, `ui.label.*` namespacing, one-time HTML escaping, translated confirm/alert behavior, stable callback identities, and delegate-failure propagation. It is installed through the application runtime; application, controller, report, workflow, and rendering consumers call its public methods directly. Source-catalog extraction and explicit-key validation recognize the direct service calls, preserving 1,973 source messages and 74 explicit key references. The nine `uiT`, `uiSource`, `currentUiLocale`, `uiLabel`, `uiLabelHtml`, `translatedSourceText`, `translatedSourceHtml`, `uiConfirm`, and `uiAlert` compatibility functions are removed, and release verification prevents them from returning.
- UiLocalizationService now also owns locale-direction resolution through `localeDir(locale())` with exact `ltr` fallback and delegate-failure propagation. Project-report and Quality Passport generators call its locale, direction, source-text, and HTML-safe source methods directly, preserving document `lang`/`dir`, interpolation, one-time escaping, CSP/meta structure, redaction, report content, filenames, and status outcomes. The four `reportLocale`, `reportDir`, `reportText`, and `reportHtml` aliases are removed, and release verification prevents them from returning.
- A checked ReportPresentationService now owns escaped ordered lists and localized empty states, lexicographically ordered count tables, localized quality-category ordering, trimmed credential-safe label fallbacks, and the bounded 50-row QA-details table with its exact localized headers and fallback cells. It is installed through the application runtime with localization, escaping, redaction, quality-category, and QA-message/fix-hint boundaries injected. Project Report and Quality Passport generators call its immutable methods directly; source-catalog extraction scans its localized report strings, focused tests characterize deterministic markup and delegate failures, and release verification prevents the five removed `reportListHtml`, `reportCountTableHtml`, `qualityCategoryCountTableHtml`, `reportSafeLabel`, and `reportQaChecksTableHtml` helpers from returning.
- A checked ReportDocumentCompositionService now owns the complete normal/anonymized Project Report and Quality Passport documents: doctype, locale/direction, restrictive CSP, inline style, section order, cards/tables/lists, fallback data, sensitive-label handling, anonymized substitutions and omissions, 10-event/20-risk/three-reason bounds, and localized header/footer copy. It is runtime-installed with localization, ReportPresentationService, escaping, redaction, profile/quality/validation, language, and date boundaries injected. App and workflow consumers call its immutable methods directly; source-catalog extraction scans its localized templates, three complete-document SHA-256 fixtures pin byte-stable output, and release verification prevents `projectReportHtml` or `qualityPassportHtml` from returning to `app.js` or the workflow driver.
- A checked ReportDataService now owns flush-before-read synchronization; exact linked-TM, project-termbase, and activity queries; portable activity sanitization; export validation and project analysis; tag-enriched worker QA with the original fallback; Quality Passport aggregation; generated timestamps; QA, review, and activity counts with `unknown` fallbacks; TM, term, forbidden-term, and revision totals; and redacted, deterministically sorted terminology. It is runtime-installed with session, autosave, resource, repository, portable-data, reporting, worker, protected-tag, redaction, and clock boundaries injected. Project Report, anonymized report, Quality Passport, and workflow consumers call its immutable method directly; focused tests preserve success, worker-fallback, validation, and failure behavior, while release verification prevents `countBy` or `buildProjectReportData` from returning to `app.js` or the workflow driver.
- A checked ReportExportController now owns no-project eligibility, normal/anonymized option normalization, report-data invocation, shared QA/filter/validation refresh, Quality Passport-only risk/workbench refresh, safe filenames, document composition/finalization/download sequencing, exact activity payloads, note classification, activity-warning status, and report-specific primary-failure containment. It is runtime-installed with session actions, application filter state, ReportDataService, ReportDocumentCompositionService, report CSP finalization, filename/download, presentation, validation-count, activity, and status boundaries injected. Import/export events, Quality Workbench, and command-palette consumers call its immutable normal, anonymized, and Quality Passport methods directly; focused tests preserve download-before-activity order, filename/content/status policy, no-project behavior, activity warnings, and primary failures, while release verification prevents `exportProjectReport` or `exportQualityPassport` orchestration from returning to `app.js`.
- A checked DeliveryExportController now owns selected-document/type/scope policy; pending-save flushes; format-aware planning/readiness; validation blocking; localized incomplete-delivery confirmation and cancellation; Target TXT, target DOCX, bilingual DOCX, Other formats/localization, XLIFF 1.2, and XLIFF 2.2 builders; bilingual worker/fallback QA and session refresh; exact filenames/MIME types/downloads; activity plan details; warning-status policy; and format-specific failure containment. It is runtime-installed with session/application/autosave, document/term/delivery/localization/confirmation, QA/tag/worker, format-builder, filename/download, presentation, activity, and status boundaries injected. ImportExportController and 27 workflow call sites consume its immutable methods directly, including the public delivery gate; focused tests preserve every format family, strict selected-type behavior, project-wide bilingual behavior, worker fallback, incomplete cancellation, activity warnings, and failures, while release verification prevents the 12 shared helpers or six exporters from returning to `app.js` or the workflow driver.
- A checked ProjectResourceTransferController now owns current-project TMX import/export, TBX import/export, and CSV/XLSX term-list import orchestration: size guards, decoded reads, exact progress/yield sequencing, parser options, workbook-versus-text routing, indexed batch persistence, linked-project dirtiness, TM/term refresh order, call-time resource selection, project-linked queries, builders, filenames/XML downloads, optional activity warnings, and export-specific failure containment. It is runtime-installed with session, file/progress, parser, repository, resource-selection/dirtiness, refresh, builder/download, activity, and status boundaries injected. ImportExportController and workflow characterization call its five immutable methods directly; focused tests preserve no-project, oversize, parser failure, singular/plural progress, call-time termbase selection, filtering, metadata, download-before-activity order, and status behavior, while release verification prevents the five local actions or shared term-list parser from returning to `app.js` or the workflow driver.
- A checked ResourceLibraryImportController now owns Resources-dashboard TMX, TBX, and CSV/XLSX term-list import orchestration: size-first validation, trimmed resource names, call-time friendly-language normalization, required-field alerts, decoded reads, exact progress/yield sequencing, parser metadata, workbook-versus-text routing, indexed batch persistence, linked-project dirtiness, non-rendering/non-focusing resource selection, resource/project-term refresh order, and exact success status. It is runtime-installed with form/input normalization, file/progress, parser, repository, resource-selection/dirtiness/refresh, localized-alert, and status boundaries injected. ResourcesController and workflow characterization call its three immutable methods directly; focused tests preserve validation returns, oversize/parser failures, singular/plural progress, TM versus termbase refresh policy, and immutable API behavior, while release verification prevents the three local Resources import handlers from returning to `app.js` or the workflow driver.
- A checked ResourceMutationController now owns TM-entry and term-entry save patches, linked-project dirtiness, resource/project-term refresh, reversible entry and whole-resource delete command construction/execution, project command identity, Trash-entry derivation/synchronization, optional terminology refresh, Undo rendering, secondary refresh-warning containment, and exact boolean/status outcomes. It is runtime-installed with session, repository, resource lookup/dirtiness/refresh, command factory/bus, Trash synchronization, presentation, test-hook, logger, and status boundaries injected. ResourcesController, terminology suggestions, and workflow characterization call its five immutable methods directly; focused tests preserve primary failures, missing-command handling, descriptor/affected-ID policy, warning copy, whole-resource label failure propagation, and hard-delete exclusion, while release verification prevents the six local mutation functions from returning to `app.js` or the workflow driver.
- A checked ResourceLibraryExportController now owns standalone Resources-dashboard TMX/TBX export lookup, branch selection, builder metadata, safe filenames, XML downloads, singular/plural status, and contained lookup/builder/download failures. It is runtime-installed with resource descriptor/item lookup, TMX/TBX builders, filename normalization, download, and status boundaries injected. ResourcesController and workflow characterization call its immutable exporter directly; focused tests preserve synchronous return behavior, exact filenames/MIME/status copy, download-before-status order, and immutable API validation, while release verification prevents `exportResource` from returning to `app.js` or the workflow driver.
- A checked ResourcesPresentationService now owns Resources TM/termbase empty states, dashboard cards, safe localized metadata, action labels/ARIA/data attributes, detail visibility/headers/counts, batched TM/term row construction, editable values, forbidden-term state, and safe HTML/text boundaries without taking listener ownership. It is runtime-installed with dashboard/detail elements, document construction, resource summary/descriptor/item lookup, localization, formatting, safe-display, escaping, and sanitized replacement boundaries injected. ResourcesController calls its immutable renderer directly; focused tests preserve cards, actions, visibility, counts, escaping, row order/fields, batched replacement, and checked API behavior, while release verification prevents the eight local rendering helpers from returning to `app.js` or the workflow driver.
- A checked ResourceCatalogService now owns delimiter-safe resource-key serialization/deserialization, unnamed and language-pair fallbacks, grouped counts, latest-update selection, deterministic name/language-pair ordering, TM/termbase field selection, exact project-language matching, selected-but-empty fallback summaries, and duplicate suppression. It is runtime-installed with current Resources state injected. ResourcesController, Resources presentation, standalone export/mutation, project resource pickers, and workflow characterization call its four immutable methods directly; focused tests preserve `::` names, explicit/derived language pairs, grouping/order, record immutability, matching, empty-state fallback, and checked API behavior, while release verification prevents the four local catalog helpers from returning to `app.js` or the workflow driver.
- A checked ProjectResourceSelectionController now owns normalized project-dialog language-pair reads, create/edit TM/termbase picker composition, safe localized option/count/empty-state markup, selected/main controls, no-language early return, checked-name collection, new-resource precedence, legacy/default TM/TB fallbacks, valid-main correction, existing link-ID preservation, generated IDs, and TM main/reference versus termbase link construction. It is runtime-installed with dialog/form/list elements, session/mode, language normalization, project-resource selectors, catalog matching, localization/safe presentation, name cleanup/deduplication, and ID generation injected. ProjectDialogController, frequent-pair presentation, project create/update, and workflow characterization call its three immutable methods directly; focused tests preserve create/edit presentation, defaults, ordering, IDs, roles, and checked API behavior, while release verification prevents the five local helpers from returning to `app.js` or the workflow driver.
- A checked ProjectLanguagePairShortcutsController now owns descending recent-project ordering, code normalization, incomplete-pair rejection, stable pair deduplication, four-recent/six-total bounds, recent-before-bundled-default precedence, current-pair active state, safe shortcut attributes/labels, and absent-root containment. It is runtime-installed with shortcut root, project list, current resource-selection values, language normalization/display, bundled defaults, escaping, and safe replacement injected. ProjectDialogController calls its immutable renderer directly; focused tests preserve ordering, normalization, deduplication, bounds, active state, safety, absent-root behavior, and checked API validation, while release verification prevents the two local shortcut helpers from returning to `app.js` or the workflow driver.
- A checked LanguageInputService now owns canonical language-code normalization with Intl/manual fallback, configured-name and catalog caching, localized/Intl display names, stable accent/punctuation-insensitive lookup, aliases and friendly label/name/code normalization, display formatting, input mutation flags, live-sync eligibility, pair display/placeholders, and all three safely escaped shared datalists. It is runtime-installed with bundled entries/aliases, redaction, localization/locale, navigator/Intl, datalist elements, escaping, and sanitized replacement injected. Project/resource dialogs, filters, reporting, shortcut presentation, AI settings/forms, import orchestration, and workflow characterization call its immutable API directly; eight focused tests preserve canonicalization, deduplication/order/caching, every input form and mutation flag, live sync, pair formatting, datalist shapes, absent elements, checked boundaries, and delegate behavior, while release verification prevents the 14 local helpers and two caches from returning to `app.js` or the workflow driver.
- A checked ProjectLanguageContextController now owns current-project and explicit pair display, normalized `source::target` keys, target-language selection, desktop spellchecker synchronization, duplicate suppression, absent-bridge and rejection containment, and target-editor `lang` assignment/removal. It is runtime-installed with current-project, checked LanguageInputService, desktop bridge, and warning boundaries injected; the synchronization cache now lives inside the controller and preserves update-before-bridge/no-retry behavior. Project summaries/filters/cards/metadata, editor rendering, project switches, and concordance presentation call its immutable methods directly; six focused tests preserve empty/current/explicit fallbacks, exact desktop arrays/results/warnings/cache behavior, element mutation, checked boundaries, and immutable API behavior, while release verification prevents the six local helpers, obsolete display-name helper, and coordinator cache field from returning.
- A checked ProjectDocumentStatisticsService now owns the exact document-progress record shape, status/word accumulation, rounded confirmation percentage, per-document map construction, metadata-only/default/missing-document buckets, aggregate totals, and exact single-document totals. It is runtime-installed with project documents, EditorSessionStore segments, and cached source-word counting injected. Project-home totals and file cards call its immutable methods directly; seven focused tests preserve independent empty records, status and unknown-status behavior, word-count calls, zero and rounded percentages, explicit/default document lists, map order, non-mutation, aggregate totals, exact document-ID matching, checked boundaries, and immutable API behavior, while release verification prevents the six local statistics helpers from returning to `app.js` or the workflow driver.
- A checked ProjectDocumentCatalogService now owns manifest-first document reconciliation, invalid/duplicate manifest-ID rejection, metadata-only and segment-derived documents, the `default-document` bucket, manifest precedence, project source-file/`"Document"` name fallback, lowercased `docx` type fallback, exact per-document segment filtering, current selected-document/all-segment scoping, and nullable selected-document lookup. It is runtime-installed with project, manifest, EditorSessionStore segment, ApplicationNavigation selection, and type-normalization boundaries injected. Project-home/file views, delivery exports, AI/context selectors, QA/report scope, duplicate-file checks, statistics composition, and workflow characterization call its immutable methods directly; six focused tests preserve order, fallbacks, scope, selection, non-mutation, checked boundaries, and immutable API behavior, while release verification prevents the five local catalog/selection helpers from returning to `app.js` or the workflow driver.
- A checked TextEncodingInputService now owns configured import-encoding option order and safe markup, absent-option Auto/UTF-8 fallback, empty configured-array behavior, absent-selector containment, reset-to-`auto`, selected-value/empty fallback, and fresh `{ encoding }` decoding options. It is runtime-installed with the encoding selector, compatibility encoding options, escaping, and safe replacement boundaries injected. Localization/XLIFF parsing, JSON/resource decoding, generic import-text reads, and startup rendering call its immutable methods directly; five focused tests preserve the exact UI-to-decoder policy and checked API, while release verification prevents the three local encoding-input helpers from returning to `app.js` or the workflow driver.
- A checked ProtectedTagInspectionService now owns stored/source-detected tag reconciliation, malformed stored-tag filtering, stored-first duplicate multiplicity, label-before-text display fallback, exact target occurrence accounting, missing-tag order, target-tag detection, and the nonblank-target warning predicate. It is runtime-installed with protected-tag detection injected. Source markup, tag trays, target previews, warning rows/messages, QA/report/import/controller boundaries, and workflow behavior consume its five immutable methods directly; six focused tests preserve detected/stored branches, reconciliation, non-mutation, display, missing/target/warning policy, and checked API behavior, while release verification prevents the five local inspection helpers from returning to `app.js` or the workflow driver.
- A checked ProtectedTextReplacementService now owns input coercion, empty-find no-op, case-sensitive and normalized literal replacement, global regex flags, native invalid-pattern propagation, empty-match rejection, counts, protected-token sorting, overlap skipping, exact token passthrough, surrounding chunk/tail replacement, and aggregate counts. It is runtime-installed with protected-tag detection and case normalization injected. TargetReplacementController and both protected/locale-stable workflow characterizations call its immutable transformer directly; six focused tests preserve literal, regex, error, detector-ordering side effect, token, overlap, count, and checked API behavior, while release verification prevents the two local transformation helpers from returning to `app.js` or the workflow driver.
- A checked SegmentProvenanceService now owns trimmed allowlisted AI-review risk, provider/model AI-draft classification, nonempty-array suggestion classification, AI badge class/text/model title policy, TM numeric conversion/nonfinite fallback/rounding/clamping, zero-score presence, trimmed TM names, and named/unnamed TM badge metadata. It is runtime-installed with checked UI localization injected. AI-state filters and segment-row provenance/risk presentation call its immutable methods directly; six focused tests preserve malformed/empty fallbacks, localization calls, score boundaries, badge records, checked boundaries, and immutable API behavior, while release verification prevents the seven local provenance helpers from returning to `app.js` or the workflow driver.
- A checked SegmentFilterService now owns selected-document, review, AI-provenance, status, and query predicate composition; source/target/both literal and regex matching; invalid-regex containment; project and visible index generation; revision-keyed visible-index/position caching; explicit invalidation; and position/first-visible fallbacks. Editor, navigation, target editing/replacement/producer, TM/AI/review, structural, and workflow consumers call its immutable API directly; seven focused tests preserve predicate ordering, case and scope behavior, exact cache identity/key refresh, invalidation, index positions, fallbacks, checked boundaries, and API immutability, while release verification prevents the ten local helpers and two coordinator cache fields from returning to `app.js` or the workflow driver.
- A checked SegmentProgressService now owns whitespace word counting, per-record source-identity WeakMap caching, cache refresh after source mutation, project-card totals/status/word/percentage aggregation, exact active-project summary composition, EditorSessionStore cache-validity checks, incremental confirmed-status deltas, confirmed-count clamping, fallback recomputation, and summary replacement sequencing. Project summaries, project-document statistics, and active editor progress call its immutable API directly; seven focused tests preserve empty/malformed fallbacks, cache behavior, exact summary shapes, project identity, delta eligibility, extra cached fields, clamping, checked boundaries, and API immutability, while release verification prevents the four local helpers, WeakMap, and coordinator delta policy from returning to `app.js` or the workflow driver.
- A checked SegmentTargetStateService now owns non-enumerable hidden fields, target-history initialization, 30-second typing coalescing, 25-entry history capping, target/status normalization, non-pretranslation TM provenance clearing, deep-cloned reversible patch capture/application, absent-versus-present optional fields, history-baseline reset, revision/timestamp mutation, and filter-invalidating touches. Editor, confirmation, target replacement/producer, TM/AI/review, structural, command restoration, import/reload, and workflow consumers use its immutable API directly; eight focused tests preserve descriptors, fallbacks, identities, metadata, coalescing, capping, patch isolation/presence, apply fallbacks, touch suppression, checked boundaries, and API immutability, while release verification prevents ten local helpers and two constants from returning to `app.js` or the workflow driver.
- A checked SegmentCommandRestorationController now owns single and batch target-patch restoration, single and batch full-snapshot restoration, affected-segment and batch-shape validation, deep-cloned before-state capture, finite snapshot and exact patch revision sequencing, fresh timestamps, autosave clearing, single/batch persistence, active-segment fallback, grid/inspector/caret/focus recovery, presentation refresh, workspace dirtiness, navigation-after-redo, and exact in-memory rollback after primary failure. EditorSessionStore records, SegmentTargetStateService, AutosaveService, repository writes, application navigation, rendering, editor context, workspace state, focus, and clocks remain injected boundaries; nine focused tests preserve every success/failure branch, active selection, clone isolation, validation message, checked boundary, and immutable API, while release verification prevents the five local restoration helpers from returning to `app.js` or the workflow driver.
- A checked SegmentConfirmationStateService now owns the confirmation history transition, confirmed-status assignment, `needs-review`-only clearing, revision/filter touch, exact identity-preserving own-key snapshot restoration, and persisted-rollback revision/timestamp preparation. SegmentTargetStateService and the clock remain injected; confirmation UI/busy/command orchestration, command restoration, persistence, TM/activity effects, navigation, rendering, workspace, status, and failure hooks remain separate. Five focused tests preserve target/history arguments, unrelated review state, the original nullish failure, enumerable/non-enumerable/symbol key semantics, current numeric coercion, checked boundaries, and immutable API, while release verification prevents the three local confirmation-state helpers from returning to `app.js` or the workflow driver.
- A checked SegmentTmSaveController now owns direct segment/project/source/target eligibility, exact untrimmed translation-memory payload construction, main-TM selection, repository persistence, project-scoped workspace dirtiness, active-segment routing, match refresh, optional success reporting, reported failure containment, and silent rejection propagation. EditorSessionStore project and active-segment selection, TM naming/repository/match refresh, workspace, status, and the checked test-only failure hook remain injected; confirmation, command-palette, Save-to-TM button, and workflow consumers call its immutable methods directly. Seven focused tests preserve early returns, default/explicit project reads, payload and result identity, effect order, refresh failures after durable saves, status copy, silent behavior, checked boundaries, and immutable API, while release verification prevents the two local direct-save helpers from returning to `app.js` or the workflow driver.
- A checked AiPretranslationController now owns local/hosted AI runtime validation, unavailable-provider handling, external-send and overwrite consent, pending-save flush, scope/eligible-segment selection, progress/running/AbortController lifecycle, provider-domain invocation and prompt-context routing, mid-batch cancellation rollback, target history/revision preparation, redacted atomic command persistence, best-effort activity/repository/sidebar refreshes, workspace/status/presentation updates, exact primary-failure rollback, and unconditional lifecycle cleanup. EditorSessionStore records, provider adapters/registry, settings and prompt/context services, CommandBus implementation, repository, mutation/history adapters, command restoration, selection, presentation, activity, workspace, and status remain injected boundaries; AI administration, manual/TM target changes, confirmation, structural edits, quality/review saves, and the other AI review/edit command families remain separate.
- A checked AiReviewController now owns active-segment and batch AI-review eligibility, runtime/provider validation, external-send consent, glossary context routing, risk normalization, stable attributed comments, visible/document/project/selected scope selection, locked/confirmed/source/target safeguards, progress/running/prompt/AbortController lifecycle, per-segment failure containment, partial-result cancellation, persistence/reload sequencing, best-effort activity logging, presentation/workspace/status updates, exact primary-failure rollback, and final cleanup. EditorSessionStore records, provider adapters/registry, settings and prompt construction, repository, mutation/history adapters, selection, presentation, activity, workspace, status, labels, IDs, clocks, and redaction remain injected boundaries; manual review/quality decisions, pretranslation, tag repair, target editing, confirmation, and structural edits remain separate.
- A checked AiTagRepairController now owns active-segment and batch protected-tag-repair eligibility, missing-tag detection, runtime/provider validation, external-send consent, protected-token routing, visible/document/project/selected scope selection, locked/confirmed/source/target safeguards, normalized non-overwriting suggestion construction, progress/running/prompt/AbortController lifecycle, per-segment failure containment, partial-result cancellation, persistence/reload sequencing, best-effort activity logging, presentation/workspace/status updates, exact primary-failure rollback, and final cleanup. EditorSessionStore records, provider adapters/registry, settings and protected-token prompt services, repository, mutation/history adapters, general suggestion storage, selection, presentation, activity, workspace, status, IDs, and redaction remain injected boundaries; target editing, confirmation, pretranslation, review, alternatives, and structural edits remain separate.
- A checked AiAlternativesController now owns active-segment and batch target-alternatives eligibility, runtime/provider validation, external-send consent, glossary/protected-token routing, variant-mode propagation, empty/current-target duplicate filtering, normalized multi-suggestion construction, visible/document/project/selected scope selection, locked/confirmed/source/target safeguards, progress/running/prompt/AbortController lifecycle, per-segment failure containment, partial-result cancellation, persistence/reload sequencing, best-effort activity logging, presentation/workspace/status updates, exact primary-failure rollback, and final cleanup without overwriting target text. EditorSessionStore records, provider adapters/registry, settings and prompt/context services, repository, mutation/history adapters, general suggestion storage, selection, presentation, activity, workspace, status, IDs, and redaction remain injected boundaries; target editing, confirmation, pretranslation, review, tag repair, terminology application, and structural edits remain separate.
- A checked AiTerminologyApplicationController now owns active-segment and batch terminology-application eligibility, runtime/provider validation, no-term short-circuiting, external-send consent, glossary/protected-token routing, unchanged-result filtering, normalized non-overwriting suggestion construction, visible/document/project/selected scope selection, locked/confirmed/source/target safeguards, progress/running/prompt/AbortController lifecycle, per-segment failure containment, partial-result cancellation, persistence/reload sequencing, best-effort activity logging, presentation/workspace/status updates, exact primary-failure rollback, and final cleanup. EditorSessionStore records, provider adapters/registry, settings and prompt/context services, repository, mutation/history adapters, general suggestion storage, selection, presentation, activity, workspace, status, IDs, and redaction remain injected boundaries; target editing, confirmation, pretranslation, review, tag repair, alternatives, draft polish/adaptation, and structural edits remain separate.
- A checked AiDraftEditingController now owns active-segment and batch polish/adaptation eligibility, operation-specific runtime/provider validation and external-send consent, TM/termbase/style/protected-token context routing, adaptation-mode propagation, unchanged-result filtering, normalized non-overwriting suggestion construction, visible/document/project/selected scope selection, locked/confirmed/source/target safeguards, progress/running/prompt/AbortController lifecycle, per-segment failure containment, partial-result cancellation, persistence/reload sequencing, operation-specific best-effort activity logging, presentation/workspace/status updates, exact batch primary-failure rollback, and final cleanup. EditorSessionStore records, provider adapters/registry, settings and prompt/context services, repository, mutation/history adapters, general suggestion storage, selection, presentation, activity, workspace, status, IDs, and redaction remain injected boundaries; target editing, confirmation, pretranslation, review, tag repair, alternatives, terminology application/extraction, and structural edits remain separate.
- A checked AiTerminologyExtractionController now owns active-segment and batch term-extraction eligibility, runtime/provider validation, external-send consent, selected/visible/project/document/untranslated source scope, provider progress/running/prompt/AbortController lifecycle, per-segment failure containment, partial-result cancellation, extracted-term aggregation, injected deduplication/storage sequencing, active/batch activity logging, best-effort resource/sidebar refreshes, presentation/workspace/status updates, and final cleanup. EditorSessionStore records, provider adapters/registry, settings and prompt services, term normalization/deduplication/storage, termbase selection, repositories, activity, workspace, status, and presentation remain injected boundaries; target editing, terminology application, project-brief generation, and other AI commands remain separate.
- A checked AiProjectBriefController now owns project eligibility, local/hosted AI settings persistence and runtime/provider validation, external-send consent after bounded document/sample/term context selection, provider prompt/running lifecycle, generated-brief normalization, project/list snapshots, append-only style-guide mutation, project persistence and synchronization, administration-form refresh, best-effort activity logging, output/workspace/status updates, exact primary-failure rollback, and final cleanup. EditorSessionStore records, provider adapters/registry, settings and prompt construction, document/sample/term context selectors, project repository, AI administration, activity, workspace, status, and presentation remain injected boundaries; target editing, terminology commands, pretranslation, suggestion application, and direct OpenAI suggestion requests remain separate.
- A checked AiSuggestionApplicationController now owns active-segment and suggestion eligibility, confirmed/locked safeguards, pending-save flushes, ApplyAiSuggestion command construction/execution, first-apply target/status/provenance/review mutation, repository persistence, best-effort activity logging, apply-and-next navigation, Undo/Redo snapshot restoration with monotonic revisions, sidebar and editor presentation, workspace/status updates, exact primary-failure rollback, and restoration-failure recovery. EditorSessionStore records, suggestion records, CommandBus implementation, target/history mutation adapters, repository, selection, presentation, activity, workspace, and status remain injected boundaries; suggestion creation/storage, direct OpenAI requests, provider adapters, AI administration, ordinary target editing, and batch AI commands remain separate.
- A checked AiOpenAiSuggestionController now owns direct OpenAI project/segment and settings eligibility, the offline-before-key safeguard, explicit external-send consent with optional local-context disclosure, project/list/key snapshots, settings persistence and list synchronization, key-memory policy, setup rollback with rollback-write warnings, TM/termbase context and provider request routing, shared suggestion-storage result handling, presentation/workspace/status updates, and request-failure containment. EditorSessionStore records, AI administration, settings normalization, OpenAI transport, context lookup, key storage, project repository, shared suggestion persistence, presentation, workspace, and status remain injected boundaries; general suggestion normalization/storage, suggestion application, local/hosted AI commands, and provider adapters remain separate.
- A checked AiSuggestionPersistenceController now owns the portable/redacted suggestion storage shape, default IDs/provider/status/origin/scope/review-state/date normalization, finite confidence, bounded explanation/context disclosure, append-only segment mutation, revision/history and pending-save sequencing, repository persistence, best-effort activity logging, suggestion/history presentation, workspace/status outcomes, activity-warning containment, and exact primary-failure rollback. Segment records, repository implementation, mutation/history adapters, redaction, IDs, clocks, activity, presentation, workspace, and status remain injected boundaries; provider requests, producer-specific orchestration, direct OpenAI setup, suggestion application, EditorSessionStore, and AI administration remain separate.
- A checked AiSettingsPersistenceController now owns global/local form and secret reads, normalized project AI settings with local-provider fields, endpoint validation, OpenAI/local credential update eligibility, project/list/key snapshots, project repository sequencing, selected-project/list synchronization, credential-memory policy, activity storage labels, best-effort activity warnings, editor/workspace/status updates, exact project/list/key rollback, and rollback-write warnings. Form state, settings normalization, local project-field mapping, endpoint policy, provider identity, key stores, EditorSessionStore records, project repository, activity, presentation, workspace, and status remain injected boundaries; provider requests, provider connection/model administration, suggestion commands, and target editing remain separate.
- A checked AiProviderAdministrationOperationsController now owns local/hosted provider connection validation and invocation, connection/status copy, OPUS-CAT discovered-endpoint persistence and setup-help routing, desktop LM Studio eligibility/start/retry, model discovery and selected-model availability decisions, Ollama pull eligibility/progress/refresh, and provider-error containment. Provider adapters/registry, settings persistence/runtime policy, endpoint normalization, desktop bridge, model records, AI administration presentation, project records, and status stores remain injected boundaries; prompt testing, batch commands, credential storage, and project AI-settings persistence remain separate.
- A checked AiPromptTestController now owns single-prompt project/running/busy/source eligibility, settings/runtime/provider and mode-capability validation, mode-aware external-share consent, translation-versus-generic provider routing, exact request payload selection, prompt-busy lifecycle, output normalization, command-centre/output presentation, status decisions, provider-error containment, and unconditional busy cleanup. Prompt construction and contextual labels, provider adapters/registry, settings persistence/runtime policy, project/sample records, AI administration presentation, batch commands, and connection/model administration remain injected boundaries.
- A checked AiPromptPreviewController now owns every prompt-mode label/system/consent-context mapping, explicit sample versus active-source fallback, preview-segment/tag synthesis, matching-term fallback with the 12-term bound, protected-token/style/variant/adaptation/empty-TM context, optional nearby segments, project-brief document/sample/term context, command-specific prompt-builder routing, request metadata, and preview rendering. AI administration form state, project/session/term/document records, prompt builder implementations, provider execution, settings persistence/runtime policy, and status remain injected boundaries.
- A checked AiTermCandidatePersistenceService now owns existing-term lookup for the active language pair/termbase, stable case-insensitive source/target deduplication against stored and repeated candidates, candidate note/forbidden-state construction, ordered sequential saves, saved-record aggregation, linked-project dirtiness after successful completion, and duplicate counts. Term repository implementation, project/resource records, language normalization, terminology provider/batch orchestration, presentation, activity, workspace storage, and status remain injected boundaries; partial save failure still propagates before linked-project dirtiness exactly as before.
- A checked AiSegmentContextService now owns project-level termbase/TM context eligibility, exact language/resource/source/limit lookup parameters, existing warning containment, settings/segment fallbacks, and ordered nonblank nearby-segment selection from the active document. Project/resource records, settings normalization, term/TM repository implementations, segment storage, AI command controllers, presentation, and status remain injected boundaries.
- A checked AiScopeSelectionService now owns selected/visible/project/untranslated/default terminology scopes, project-versus-document pretranslation sources, ordered selected/visible stable-ID options, and bounded project-brief samples with active-document preference, project fallback, blank-source filtering, and empty-target fallback. Project/session records, form settings, filter computation, AI providers, command controllers, presentation, and status remain injected boundaries; AiTerminologyExtractionController consumes the shared selection instead of duplicating it.
- A checked ExternalAiConsentService now owns stable trimmed/deduplicated disclosure labels, zero/one/two/Oxford-comma list grammar, mandatory project-instruction and optional selected/source-text disclosure order, exact provider-aware confirmation copy, and unchanged accepted/canceled result propagation. The confirmation primitive, provider selection, command eligibility/timing, controllers, presentation, and status remain injected boundaries; all ten local/hosted and direct OpenAI command families consume the service.
- A checked AiProviderPresentationService now owns local, Ollama-cloud, hosted-key, and network privacy copy; safe endpoint-path reduction; exact model/translation endpoint summaries for all 18 provider registry positions; model-pull eligibility; ordered capability labels and fallback; localized badge order; and provider/preset/model/base/guidance/nearby-context summary fallbacks. Provider adapters and registry lookup, preset and URL builders, locality/key/guidance policy inputs, localization primitives, credential storage, administration rendering, commands, and status remain injected boundaries.
- A checked AiCredentialStorageService now owns browser-only global OpenAI and provider/base-scoped local/hosted AI key access; safe local/session storage acquisition, read/write/clear warning containment, session-before-local key precedence, remember-versus-tab routing, blank-key clearing, legacy-key cleanup, exact snapshots/restoration, primary-failure rollback, safe-restore warnings, and storage labels. Browser storage objects, settings and provider/base normalization, test-failure injection, administration presentation, redaction, status, project persistence, and command orchestration remain injected boundaries. AI settings, direct OpenAI suggestions, provider controls, and workflow characterization consume this service, and release verification rejects the coordinator-private storage helpers returning.
- A checked AiRuntimeSettingsService now owns normalized global project AI settings; redacted provider/model/style/local metadata; local mode allowlists and concurrency/timeout bounds; local-provider project/form/language composition; hosted OpenAI-compatible endpoint eligibility; typed, provider-scoped, and OpenAI-global credential selection; and provider-key readiness with action-specific copy. The administration controller, local settings store, language and provider primitives, endpoint allowlist predicate, credential service, project records, redaction primitive, presentation, persistence, status, and commands remain injected boundaries. Every AI command/controller consumes the checked methods, and release verification rejects the five former coordinator settings/runtime helpers returning.
- A checked AiProviderFormController now owns provider preset grouping and selection; provider, model, progress, output, and full command-centre view-model composition; provider/preset/base-URL/general-form/language transitions; live language-field synchronization; key-clear intent routing; redacted connection status; and exact presentation refresh sequencing. The AI administration controller retains DOM and listener ownership, while provider adapters/registry, runtime-settings and credential policy, provider presentation, project/session records, OPUS-CAT help, persistence, status storage, localization, and AI commands remain injected boundaries. Release verification rejects the former coordinator-private provider-form render and transition helpers returning.
- A checked AiSuggestionListController now owns the localized muted empty state; bounded newest-first four-card composition; provider/model/confidence fallbacks; safe provenance, before/after inspection, and explanation DOM; and separate Apply versus Apply and next intent routing. Suggestion creation/normalization/storage, application commands and Undo/Redo, target/history mutation, persistence, navigation, project/session records, localization/date primitives, workspace, status, and producer-specific AI requests remain injected boundaries. All suggestion producers, application refreshes, and the contextual inspector consume the checked renderer, and release verification rejects suggestion-list DOM or button ownership returning to `app.js`.
- AI suggestion and segment-context consumers now route directly to the checked AiSuggestionPersistenceController, AiSuggestionApplicationController, and AiSegmentContextService public methods. Suggestion-list and producer consumers created before those controllers retain exact rest-argument late binding, while later controllers and workflow characterization call the public boundaries directly. The six compatibility forwarders for suggestion normalization, append, application, glossary terms, TM matches, and nearby segments are removed, and release verification prevents them from returning.
- AI command consumers now resolve configured providers directly through AiProviderService while preserving the call-time local-form fallback; the provider form late-binds LM Studio eligibility directly to AiProviderAdministrationOperationsController; and provider-form, administration, and workflow consumers call OpusCatHelpController visibility/open methods directly with construction order, optional chaining, open results, and focus return preserved. The four configured-provider, LM Studio, and OPUS-CAT help forwarding functions are removed, and release verification prevents them from returning.
- AiSegmentContextService now also owns direct-OpenAI explicit-settings resource-context composition: concurrent TM/termbase lookup start, stable `[tmMatches, terms]` result order, project language and selected-resource query construction, independent opt-outs, and rejection propagation. AiOpenAiSuggestionController consumes the public method directly, preserving unsaved global-form setting precedence and the TM query without an added result limit; `aiContextForSegment` is removed from `app.js`, and release verification prevents this orchestration from returning.
- A checked AiLocalSettingsPersistenceController now owns command-time local/hosted AI settings sequencing: no-project form fallback, non-throwing unsaved endpoint rejection, local settings store write, normalized merge with existing project AI settings, repository persistence, selected-project and project-list synchronization, workspace dirtiness, and silent versus visible saved status. Form composition/normalization and endpoint policy, the local settings store, project repository, EditorSessionStore records, provider/runtime selection, AI commands, workspace state, and status state remain injected boundaries. All ten local/hosted command controllers consume the silent method, and release verification rejects the former coordinator helper and sequencing returning.
- A checked AiCommandLifecycleCoordinator now owns lifecycle adapters for the seven batch-capable AI command controllers plus uniquely owned prompt-only adapters for project-brief generation and prompt testing: unconditional versus defined progress mirroring, prompt-busy ownership where applicable, owner-specific AbortController capture, latest-active-controller replacement, identity-guarded running/abort clearing, exact ordered cancel routing, and the legacy fallback abort/canceled-progress/presentation/status path. Each command controller retains its own running, progress, AbortController, provider, command, persistence, and status behavior; shared application-state mutation, progress presentation, and status storage remain injected boundaries. Release verification rejects the former private abort variables, prompt-busy flags, repeated sync blocks, shared cancel façade, and prompt-only synchronization blocks returning.
- Provider-form and provider-administration prompt-preview consumers now use exact late-bound adapters to the checked AiPromptPreviewController, while prompt testing, editor selection refresh, and workflow characterization call its public methods directly. The seven former `localAi*` prompt-preview forwarding functions are removed, omitted-argument defaults and startup order remain unchanged, and release verification prevents the compatibility façade from returning.
- All 24 AiAdministrationController action entries now late-bind directly to their checked settings, provider-administration, prompt-test, local/hosted command, and direct OpenAI owners; the two pretranslation entries intentionally share the same controller method and the single action-error boundary is unchanged. The command palette, OPUS-CAT retry, and 41 workflow-characterization call sites use public controller methods directly. Twenty-three unique forwarding functions and three dead pre-extraction review-selection helpers are removed, while release verification preserves labels, enablement safeguards, active/batch distinctions, and construction order.
- A checked ReviewStateController now owns the compact quick review-state toggle/clear lifecycle: ChangeReviewState command sequencing, repository persistence, review-form synchronization, optional activity logging, row/workspace/status updates, exact primary-failure rollback, and command restoration. EditorSessionStore records, CommandBus implementation, domain mutation/history preparation, repository, activity, presentation, workspace, status, and localized labels remain injected boundaries; full review-metadata form saves, quality decisions, confirmation, structural split/merge, and batch changes remain separate.
- A checked ReviewMetadataController now owns full review-form normalization, optional stable comment creation, segment revision touching, pending-save clearing, repository persistence, best-effort activity logging, forced review/row/history presentation, workspace/status updates, and exact primary-failure rollback. EditorSessionStore records, domain history preparation, AutosaveService, repository, activity, presentation, workspace, status, IDs, and timestamps remain injected boundaries; quick review commands, quality decisions, confirmation, structural edits, target replacement, and batch changes remain separate.
- A checked QualityDecisionController now owns decision category/severity normalization, localized structured-comment creation, the `needs-review` transition, segment revision touching, pending-save clearing, repository persistence, quality-risk queue replacement, decision-note clearing, review/workbench/row presentation, workspace/status updates, activity-warning results, and exact primary-failure rollback. EditorSessionStore records, domain history preparation, AutosaveService, repository, risk construction, activity, presentation, workspace, status, labels, IDs, and timestamps remain injected boundaries; quick/full review metadata, quality profiles, confirmation, structural edits, target replacement, and batch changes remain separate.
- A checked QualityProfileController now owns profile normalization, selected-project and project-list snapshots, project repository sequencing, selected-record/list synchronization, quality-risk replacement, project-summary refresh, workbench rendering, workspace dirtiness, optional activity-warning status, and exact project/list rollback on primary failure. EditorSessionStore records, profile/risk domain functions, repository, summary refresh, activity, presentation, workspace, and status remain injected boundaries; quick/full review metadata, quality decisions, confirmation, structural edits, target replacement, and batch changes remain separate.
- A checked StructuralSegmentController now owns split/merge eligibility, protected-range and target-to-source cursor mapping, pending-save flushes, stable structural IDs and contiguous ordering, typed command construction/execution, protected-tag regeneration, atomic structural save/delete, selection, rendering/focus, workspace/status updates, exact first-transaction rollback, and Undo/Redo restoration. EditorSessionStore records, CommandBus implementations, domain mutation/history preparation, AutosaveService, the atomic repository boundary, rendering, selection, status, and workspace dirtiness remain injected; ordinary typing, confirmation, target replacement, and batch pretranslation remain separate.
- Structural command-palette and workflow consumers now call StructuralSegmentController directly for protected ranges, cursor mapping, split/merge eligibility, next-segment selection, split/merge execution, and controller-owned Undo/Redo restoration. Seven forwarding functions plus two dead restoration wrappers are removed with current-segment defaults, booleans, promises/results, structure safeguards, focus/selection, persistence order, and retryable Undo/Redo preserved; release verification prevents all nine from returning.
- The 6,443-line application workflow characterization driver now lives under `tests/app-workflow/` and is composed into `app.js` lexical scope only by the test renderer build. Source and artifact checks reject the driver from the production graph while the existing deep browser workflow remains unchanged.
- Checked JavaScript/JSDoc, ESLint, Prettier, Stylelint/token enforcement, import-boundary checks, Node focused tests, axe checks, and visual regression checks. Prettier now follows the repository's `.gitattributes` LF contract, so the formatting gate is reproducible on a clean checkout.
- Explicit app store, navigation, platform, repository, preference, status/job/error, feature-controller, command, palette, theme, layout, update, diagnostics, safe-DOM, and report-template boundaries under `src/`.
- A quiet semantic design system with restrained surfaces, no persistent blur, semantic light/dark roles, Balanced/Compact density, remembered inspector/layout preferences, and responsive Projects → dashboard → editor layouts.
- Contextual inspector tabs, fuzzy/grouped `Ctrl/Cmd+K` palette, recent commands, filter presets, contextual AI suggestions, separated provider administration, inspectable provenance, AI apply-and-next, and reversible AI application.
- IndexedDB schema 6 with persistent manual Trash for projects, project files, individual TM entries, individual terms, whole translation memories, and whole termbases while preserving project-package schema 5 and compatibility with existing package/backup paths.
- Transactional project/file Trash, segment confirmation/confirm-and-next, quick review-state, target replacement, segment splitting/merging, and AI-application operations with Undo/Redo. Command restores use monotonic revisions so IndexedDB's stale-write protection cannot silently reject an Undo.
- Resource deletion now uses the same bounded command/Undo/Redo model instead of immediate confirmation and hard deletion. Resource records and their token indexes are removed atomically; restoration recreates exact content and metadata, marks indexes for safe lazy rebuild, refreshes linked projects, and blocks ID or same-name/language-pair conflicts without overwriting live data or discarding the Trash item. Empty Trash remains the only irreversible deletion action.
- `SplitSegment` and `MergeSegment` use independent typed structural commands over one atomic IndexedDB boundary. Merge updates the surviving segment and deletes the merged-away segment in one transaction; Undo restores both stable IDs/order/history, Redo deletes only the merged-away segment with monotonic revisions, and a forced first-transaction failure records no command or partial state.
- Ordinary target typing now uses coalesced `EditTarget` commands: one continuous typing session creates one redacted Undo receipt while preserving the 450 ms autosave/retry path, exact target history/provenance restoration, persisted Undo/Redo, active selection, and monotonic revisions.
- Copy Source, TM-match/concordance insertion, and protected-tag insertion now use discrete redacted commands. A pending typing session is finalized below the producer command, and Undo/Redo restores target state, persistence, selection, provenance boundaries, monotonic revisions, and protected-tag caret placement.
- TM and Local/Hosted AI pretranslation now use atomic, bounded batch commands. TM applies all matched patches in one storage transaction; AI persists successful provider results only after completion, rolls back all in-memory output on cancellation, preserves confirmed/locked safeguards, and exposes one redacted Undo/Redo operation with TM/AI provenance and review-state restoration.
- Actionable service-worker update lifecycle that waits for user action and flushes pending saves before activation/reload.
- Centralized trusted DOM/report boundaries, CSP Trusted Types enforcement in Electron/Chromium, safe worker/service-worker URL construction, and no remaining unallowlisted raw `innerHTML` writes in application code.
- Local-only redacted diagnostics covering runtime, sandbox/GPU status, storage estimates, update state, and normalized errors without source, target, prompt, key, or exact-path content.
- Translator-first modernization copy in English, Catalan, and Turkish. The locale catalogs validate with no blank Catalan or Turkish messages.
- The downloadable web package now selects a self-contained `file://` renderer entry while HTTP(S) retains lazy ES modules; direct-file and hosted smoke tests exercise real New project and About interactions, and Electron keeps its static private-protocol entry.

## Measured results

- Packaged production JavaScript graph: 2,777,548 bytes across five modules, including both mutually exclusive startup paths.
- Hosted/desktop initial production `app.js`: 1,144,203 bytes minified and 305,222 bytes gzip. Its two locale chunks remain lazy.
- Direct-file web fallback `app-file.js`: 1,388,752 bytes minified and 388,264 bytes gzip; it is self-contained because browsers block module imports from `file://` and is not executed by HTTP(S) or Electron.
- Initial synchronous JavaScript is more than 40% below the recorded approximately 2.62 MB baseline. Hosted gzip remains within the 250 KiB-class budget but is 55,222 bytes above a strict 250,000-byte interpretation; the hosted entry remains 394,203 bytes above the 750 KB minified stretch target.
- Visual verification passes 81 deterministic screenshots covering 1440×900, 1366×768, and 1024×768, including an actionable import-validation failure, visible focus recovery, the local recovery panel and open workspace-status menu, populated Comments and Quality Workbench inspectors, Resources TM dashboard/detail, termbase dashboard, populated resource Trash, post-restore empty Trash, the TM threshold and OPUS-CAT help dialogs, provider administration/AI Command Centre, light/dark, inspector open/closed, Focus mode, AI, status, and compact-density states.
- Automated accessibility checks pass the deterministic Projects, import-validation error and focus-return path, actionable local recovery panel, open workspace-status menu, Resources translation-memory/termbase empty states, populated resource Trash, populated Comments and Quality Workbench states, AI provider administration/Command Centre and focus-return path, New project dialog, About dialog, TM threshold dialog, OPUS-CAT help dialog, and command-palette states with zero blocking findings. This is not a WCAG conformance claim.
- The full Electron browser suite passes security, offline shell, smoke, regression, application workflow, workspace storage, package round trip, and large-project coverage.
- The packaged Windows desktop passes artifact inspection, fuse verification, renderer OS sandbox verification, GPU-enabled startup, and the explicit GPU-disabled fallback.

## Partial roadmap packages

### P1-08 — Remaining `app.js` extraction

The new checked boundaries are active, but `app.js` remains the compatibility coordinator and is still 6,870 source lines. The 6,443-line workflow driver is external and composed only into the test graph, but the roadmap's source goal of a bootstrap under 300 lines is not met.

The synchronous dialog-lifecycle, async project-dialog, TM-threshold, OPUS-CAT help, Resources state/events and presentation, quality/review, recovery/workspace including package save, sync, backup export, and health repair, import/export/report, delivery-export, current-project resource-transfer, project resource selection and language-pair shortcuts, shared language-input, text-encoding input, portable-file input and shared import-task lifecycle, protected-tag inspection, protected-text replacement, segment AI/TM provenance, segment target state and command restoration, project-language context/spellcheck, project document-catalog/selection, and project document-statistics policy, resource catalog, resource-library import/export/mutation, AI provider-administration/command-centre, target-edit, autosave, segment-confirmation, discrete target-producer, target-replacement, TM and local/hosted AI batch-pretranslation, active/batch AI review, protected-tag repair, target alternatives, terminology application/extraction, draft polish/adaptation, project-brief generation, direct OpenAI suggestion requests, shared AI suggestion persistence, project AI-settings persistence, local-provider connection/model administration, AI suggestion application, quick review-state, full review-metadata, quality-decision, quality-profile, and structural split/merge slices are complete. All 18 AI registry positions now install through checked adapters rather than provider implementations in `ai.js`. This includes the hosted OpenAI-compatible family (Groq, Together AI, OpenRouter, Hugging Face Inference Providers, DeepInfra, and Fireworks AI), the native Responses family (OpenAI, xAI, and Azure OpenAI), native chat-completion providers (DeepSeek and Mistral), Perplexity Sonar, Google Gemini Interactions, Anthropic Messages, Cohere Chat V2, local/hosted Ollama, loopback/allowlisted OpenAI-compatible servers, and OPUS-CAT direct/bridge MTRestService. Their original registry positions, endpoints, credentials, payloads, parsing, metadata, timeout/cancellation behavior, redacted errors, consent rules, and temporary compatibility exports are characterized independently. Release verification rejects any provider object implementation or provider registration returning to `ai.js`, and now also rejects target textarea listeners, pending autosave timer state, segment-confirmation event/busy state, discrete target-producer command/event orchestration, target-replacement input/event/mutation/command/persistence/activity/presentation/rollback orchestration, TM-pretranslation busy/candidate/lookup-loop/mutation/command/persistence/activity/presentation/rollback orchestration, AI-pretranslation validation/consent/lifecycle/provider/mutation/command/persistence/activity/presentation/rollback orchestration, AI-review validation/consent/lifecycle/selection/risk/comment/mutation/persistence/activity/presentation/recovery orchestration, AI-tag-repair validation/consent/lifecycle/selection/protected-token/suggestion/persistence/activity/presentation/recovery orchestration, AI-alternatives validation/consent/lifecycle/selection/context/variant/suggestion/persistence/activity/presentation/recovery orchestration, AI-terminology-application validation/consent/lifecycle/selection/context/suggestion/persistence/activity/presentation/recovery orchestration, AI-draft-editing validation/consent/lifecycle/selection/context/mode/suggestion/persistence/activity/presentation/recovery orchestration, AI-terminology-extraction validation/consent/lifecycle/selection/provider/term-persistence/activity/presentation/status orchestration, AI-project-brief validation/consent/lifecycle/context/provider/project-persistence/administration/activity/presentation/status/recovery orchestration, AI-suggestion-application validation/command/restoration/mutation/persistence/activity/presentation/navigation/status/recovery orchestration, AI-suggestion normalization/append/persistence/activity/presentation/workspace/status/recovery orchestration, AI-settings form/normalization/endpoint/snapshot/project/list/key/activity/presentation/workspace/status/recovery orchestration, local-provider connection/model validation/provider/desktop-retry/discovery/model-state/progress/status orchestration, direct-OpenAI validation/consent/setup/key/context/provider/suggestion-storage-result/presentation/workspace/status/recovery orchestration, quick review-state command/mutation orchestration, full review-metadata mutation/persistence/activity/presentation/rollback orchestration, quality-decision normalization/mutation/persistence/risk/activity/presentation/rollback orchestration, quality-profile normalization/snapshot/persistence/risk/summary/activity/presentation/status/rollback orchestration, structural mapping/command/restoration/button ownership, shared canonicalization/catalog/input/display/datalist policy, text-encoding option/default/selection/decoding policy, portable-file validation/size/decoding/JSON/progress/failure and import-task overlap/busy/status/validation/durability policy, protected-tag reconciliation/multiplicity/display/missing/warning policy, protected-text literal/regex/token/chunk/count policy, segment AI-risk/draft/suggestion/TM-score/badge policy, project pair/key/target/desktop-sync/editor-language orchestration, document catalog/reconciliation/type/segment-scope/selection policy, or document progress accumulation/map/aggregate policy returning to `app.js`. Still required before this package is complete:

Checked replacement/autosave/confirmation/TM-pretranslation consumer wiring, remaining target-edit/autosave application and workflow direct wiring, editor-context and quality/review consumer direct wiring, EditorSessionStore getter consumer direct wiring, AppStore/FilterStore and ApplicationNavigation consumer direct wiring, UI/report-localization, shared language-input, text-encoding input, protected-tag inspection, protected-text replacement, segment AI/TM provenance, project-language context/spellcheck, project document-catalog/selection, and project document-statistics, report-presentation, report-document composition, report-data collection, report-export, delivery-export, current-project resource-transfer, project resource selection and language-pair shortcuts, resource catalog, resource-library import/export, Resources presentation, and resource mutation orchestration policy and consumer direct wiring, structural command/workflow/restoration direct wiring, target-edit selection and discrete target-producer consumer wiring, AI prompt preview/testing and direct consumer wiring, direct AI command dispatch, AI terminology-candidate persistence, AI suggestion-list presentation, shared AI segment context including direct-OpenAI explicit-settings resource composition, shared AI scope/sample selection, external-send consent, provider presentation, browser credential storage, runtime-settings composition/readiness, provider-form/command-centre orchestration, command-time local-settings persistence, shared batch-command lifecycle/cancellation, prompt-only project-brief/prompt-test lifecycle adapters, suggestion/context consumer direct wiring, and provider/help consumer direct wiring are also complete: release verification now rejects checked editor-controller, target-edit/autosave, editor-context/quality-review, EditorSessionStore selector, AppStore/FilterStore consumer, ApplicationNavigation consumer, UI/report-localization/language-input/text-encoding-input/protected-tag-inspection/protected-text-replacement/segment-provenance/project-language-context/project-document-catalog/project-document-statistics/report-presentation/report-document/report-data/report-export/delivery-export/project-resource-transfer/project-resource-selection/project-language-pair-shortcuts/resource-catalog/resource-library-import/resource-library-export/resources-presentation/resource-mutation consumer, structural, and target selection/producer forwarding functions, report-data query/analysis/QA/aggregation/count/redaction orchestration, report export eligibility/data/session/presentation/download/activity/status/failure orchestration, delivery selection/scope/plan/validation/confirmation/build/QA/download/activity/status/failure orchestration, project-resource read/parse/progress/persistence/dirtiness/refresh/query/build/download/activity/status/failure orchestration, project resource-selection values/picker/checked-name/default/main/link-ID/role orchestration, project language-pair recent/default/order/deduplication/bounds/active/safe-markup presentation, canonicalization/catalog/input/display/live-sync/pair/datalist policy, text-encoding option/default/selection/decoding policy, protected-tag reconciliation/multiplicity/display/missing/warning policy, protected-text literal/regex/token/chunk/count policy, segment AI-risk/draft/suggestion/TM-score/badge policy, project pair/key/target/desktop-sync/editor-language policy, document catalog/reconciliation/type/segment-scope/selection policy, document progress shape/status/word/percentage/map/aggregate policy, resource catalog key/descriptor/grouping/latest-update/order/matching/fallback policy, resource-library form/validation/read/parse/progress/persistence/dirtiness/selection/refresh/status orchestration, resource-library export lookup/build/filename/download/status/failure orchestration, Resources empty/card/detail/row/localization/safety/batched-DOM presentation, resource mutation patch/persistence/command/Trash/Undo/refresh/status/failure orchestration, prompt mode/context/request construction, prompt-test validation/consent/provider/lifecycle/presentation orchestration, prompt-preview or command-dispatch forwarding functions, obsolete review selectors, term-candidate lookup/deduplication/record/save-loop/workspace orchestration, suggestion-list empty/card/provenance/inspection/explanation DOM and apply-intent routing, termbase/TM eligibility/lookup/warning/nearby-segment selection or direct-OpenAI resource composition, terminology/pretranslation scope and project-brief sample policy, disclosure normalization/list grammar/question/confirmation policy, provider privacy/endpoint/pull/capability/badge/summary policy, credential storage acquisition/read/write/clear/scope/precedence/snapshot/rollback/label policy, project-setting normalization/form-composition/endpoint/runtime-key/readiness policy, provider/preset/model/command-centre view-model and form-transition sequencing, local-store/project/list/workspace/status persistence sequencing, batch lifecycle-state/AbortController identity/ordered-cancel/fallback orchestration, prompt-only private prompt-busy ownership, or the removed suggestion/context/provider/help forwarding functions returning to the compatibility façade.

The checked segment-filter slice is also complete: release verification now rejects document/review/AI/status/query predicate composition, project/visible index generation, revision-keyed cache ownership, invalidation, position lookup, first-visible lookup, and the ten removed forwarding helpers returning to `app.js` or the workflow driver. Direct editor, navigation, target, AI/TM/review, structural, rendering, and workflow consumers call SegmentFilterService.

The checked segment-progress slice is also complete: release verification now rejects source word-count normalization/caching, project-card progress aggregation, active-summary recomputation, EditorSessionStore cache-validity checks, status-delta policy, and the four removed helpers returning to `app.js` or the workflow driver. Project summaries, project-document statistics, and active progress presentation call SegmentProgressService.

The checked segment-target-state slice is also complete: release verification now rejects hidden-field descriptors, history preparation/coalescing/capping, target/status mutation, optional command-patch capture/application, revision/timestamp touch policy, filter invalidation, the ten removed helpers, and both history constants returning to `app.js` or the workflow driver. All 211 application/workflow call sites use SegmentTargetStateService directly.

The checked segment-command-restoration slice is also complete: release verification now rejects target-patch and full-snapshot restoration, revision/timestamp preparation, batch-shape and affected-segment validation, autosave/persistence/selection/presentation/focus sequencing, exact rollback, and the five removed helpers returning to `app.js` or the workflow driver. All nine application restoration consumers call SegmentCommandRestorationController directly.

The checked segment-confirmation-state slice is also complete: release verification now rejects confirmation history/status/review/touch mutation, exact own-key snapshot restoration, persisted-rollback revision/timestamp preparation, and the three removed helpers returning to `app.js` or the workflow driver. SegmentConfirmationController calls SegmentConfirmationStateService directly.

The checked direct segment-TM-save slice is also complete: release verification now rejects segment/project/text eligibility, exact TM payload/main-name/repository/workspace sequencing, active selection, match refresh, report/silent status behavior, and the two removed helpers returning to `app.js` or the workflow driver. Confirmation, palette, button, and workflow consumers call SegmentTmSaveController directly.

The checked concordance slice is also complete: release verification now rejects selection precedence/normalization, escaped highlighting, editor/project eligibility, linked-TM/language-pair filtering, newest-first ordering, localized metadata/empty-state presentation, safe result DOM, target-insertion intent, overlay/result lifecycle, and close-button/backdrop ownership returning to `app.js`. Palette, global-key, and Escape consumers call ConcordanceController directly, while the controller delegates insertion to the injected TargetProducerController boundary. Eight focused tests and the full 530-test quality gate characterize selection, escaping, filters, order, safe rendering, insertion provenance, empty/error outcomes, lifecycle idempotence, boundary validation, and immutable API behavior.

The checked active-segment navigation slice is also complete: release verification now rejects selected-index bounds/idempotence, selection/context/busy/visibility/row/prompt/context-refresh sequencing, next-open eligibility and wraparound, filter-reset/rerender/focus policy, the three removed helpers, and indirect application/workflow consumers returning to `app.js` or the workflow driver. Eight focused tests and the full 538-test quality gate characterize visible-position handling, selection no-ops, exact effect ordering, async failure propagation, later-candidate preference, wraparound, hidden-filter reset, focus, empty/no-open results, checked boundaries, and immutable API behavior.

The checked segment-draft application slice is also complete: release verification now rejects previous-status fallback, pre/post filter membership, target/status/history mutation, conditional invalidation, filter-sensitive rendering, progress/history/workspace effects, patch return, and the removed `applyTargetDraft` helper returning to `app.js`. Five focused tests and the full 543-test quality gate characterize explicit and fallback statuses, all three render branches, exact effect/return ordering, mutation-failure short-circuiting, checked boundaries, and immutable API behavior.

The checked command-palette consumer slice is also complete: release verification now rejects the three `openCommandPalette`, `closeCommandPalette`, and `renderCommandPalette` façades and indirect global-key, toolbar, or workflow consumers returning to `app.js` or the workflow driver. Both palette-search tests, the full 543-test quality gate, the deep keyboard/focus workflow, the command-palette accessibility state, 81 visual screenshots, web/desktop artifacts and smoke tests, Electron fuses, and GPU-on/off packaged startup pass with no intended behavior change.

The checked target-replacement panel slice is also complete: TargetReplacementController opens the injected `<details>` panel before synchronously focusing the find input, preserves `undefined` return and browser-error propagation, and exposes the method through its immutable API. The command-palette action calls that method directly; release verification rejects `openReplacePanel` or an indirect action target returning. Six focused controller tests and the full 544-test quality gate cover ordering, failure behavior, checked elements, immutable API, replacement commands, recovery, and activity warnings, while the full browser/release/package gates pass with no intended behavior change.

The checked application-global keyboard slice is also complete: release verification rejects `handleGlobalKeydown`, direct capture-listener registration, and shortcut/overlay routing returning to `app.js` or the workflow driver. Six focused tests and the full 550-test quality gate characterize idempotent capture lifecycle, editable-target Undo/Redo exclusion, command capability and project identity, palette overlap/optional behavior, Focus and concordance eligibility, KeyK fallback, exact event effects, Escape priority, and synchronous error timing; deep browser keyboard/focus behavior, accessibility, visual, web/desktop, fuse, and GPU-on/off gates all pass.

The checked quality-presentation slice is also complete: release verification rejects the four localized label façades, mapping literals, or indirect report/quality consumers returning to `app.js` or the workflow driver. Five focused tests and the full 555-test quality gate characterize all profile/category/severity/risk mappings, unknown and empty fallbacks, base-category precedence, one-call localization, checked boundaries, immutable API, and delegate failure propagation; i18n, report fixtures, deep browser quality/report flows, accessibility, visual, web/desktop, fuse, and GPU-on/off gates all pass.

The checked quality-workbench slice is also complete: an immutable QualityWorkbenchController now owns QA grouping, current-scope risk-queue construction and project-aware cache validation, active evidence fallback scoring, workbench view-model composition, QA-driven refresh, visible/hidden risk navigation, filter/document recovery, and next-risk ordering/wraparound. Quality review/profile/decision/report/editor/workflow consumers call the controller directly, and release verification rejects the seven removed coordinator functions returning. Eight focused tests and the full 563-test quality gate characterize default and explicit checks, queue/evidence/cache branches, refresh returns and failures, exact hidden-risk recovery order, next/wrap/no-risk behavior, checked boundaries, immutable API, and failure propagation; i18n, deep browser quality/report flows, all 15 accessibility states, 81 visual screenshots, web/desktop artifacts, fuses, and GPU-on/off packaged startup pass with no intended user-visible difference.

The checked revision-history presentation slice is also complete: an immutable RevisionHistoryPresentationService now owns all ten reason mappings and localization fallbacks, absent-list/missing-segment/empty-history states, copied newest-first ordering, the eight-card bound, exact status/date/before-after markup, escaping, and safe-HTML replacement. All fifteen autosave, command restoration, mutation, editor-context, locale-refresh, scheduled-render, and presentation consumers call the service directly; `revisionReasonLabel` and `renderRevisionHistory` are removed. Six focused tests and the full 569-test quality gate characterize every branch, exact delegate inputs and safe markup, history immutability, checked dependencies, immutable API, and failure propagation; i18n, deep browser revision/Undo/Redo flows, all 15 accessibility states, 81 visual screenshots, web/desktop artifacts, fuses, and GPU-on/off packaged startup pass with no intended user-visible difference.

The checked QA-results slice is also complete: an immutable QaResultsController now owns summary aggregation, private filter state, localized message and fix-hint resolution, summary controls, exact safe card composition, the 100-card bound, batched DOM replacement, and per-result segment navigation/rerender/focus sequencing. QA execution, reports, delivery export, locale refresh, and workflow consumers call the controller directly; `state.qaFilter`, `qaSummary`, `qaCheckMessage`, `qaCheckFixHint`, and `renderQaResults` are removed. Seven focused tests and the full 576-test quality gate characterize insertion order, matching type/severity double counts, the legacy `undefined` summary key, filter toggles, empty/no-match/populated states, exact safe markup, localization inputs, navigation timing, checked dependencies, immutable API, and failure propagation; release, i18n, deep browser QA/report flows, all 15 accessibility states, 81 visual screenshots, web/desktop artifacts, fuses, and GPU-on/off packaged startup pass with no intended user-visible difference.

The checked TM-matches slice is also complete: an immutable TmMatchesController now owns the active-segment/project guard, exact project-language and linked-TM query composition, post-query project/segment stale-result suppression, localized muted empty states, ordered safe match-card construction, batched DOM replacement, and TargetProducerController insertion intent. EditorContextController, SegmentTmSaveController, and current-project TM import call the controller directly; `refreshTmMatches` and coordinator-owned TM-result DOM are removed. Seven focused tests and the full 583-test quality gate characterize exact context-read counts and lookup inputs, missing context, both stale races, rejection timing, empty/populated states, exact markup and fallbacks, omitted button type, insertion provenance/ID fallback/synchronous failures, checked dependencies, immutable API, and rendering propagation; i18n, deep browser TMX/card/insertion/history/Undo/Redo flows, all 15 accessibility states, 81 visual screenshots, web/desktop artifacts, fuses, and GPU-on/off packaged startup pass with no intended user-visible difference.

The checked term-suggestions slice is also complete: an immutable TermSuggestionsController now owns the active-segment/project guard, exact project-language and linked-termbase query composition, post-query project/segment stale-result suppression, localized muted empty states, ordered approved/forbidden safe term-card construction, batched DOM replacement, and awaited ResourceMutationController deletion intent. Project-resource transfer, AI term extraction, EditorContextController, resource-Trash synchronization, term-form save, and workflow consumers call the controller directly; `refreshTerms` and coordinator-owned term-suggestion DOM are removed. Seven focused tests and the full 590-test quality gate characterize exact context-read counts and lookup inputs, missing context, both stale races, rejection timing, empty/populated states, exact approved/forbidden markup and fallbacks, omitted button type, term identity, delete options, awaited undefined fulfillment/rejection, checked dependencies, immutable API, and rendering propagation; i18n, deep browser TBX/CSV/card/form-save/delete/Trash flows, all 15 accessibility states, 81 visual screenshots, web/desktop artifacts, fuses, and GPU-on/off packaged startup pass with no intended user-visible difference.

The checked term-form slice is also complete: an immutable TermFormController now owns project/required-field eligibility, selected-versus-primary termbase choice, the exact untrimmed repository payload, linked-project dirtiness, form reset and termbase-select rendering, ordered best-effort project-term and term-suggestion refresh, durable refresh warnings, success/failure status, the injected pre-save test seam, and idempotent submit lifecycle. The form is mounted once at the former listener position and both workflow consumers call the controller directly; `saveTermFromForm` and coordinator-owned submit handling are removed. Eight focused tests and the full 598-test quality gate characterize short-circuit reads, exact payload and project reads, selected/primary termbase behavior, optional forbidden input, persistence/dirtiness/reset/render/refresh/status ordering, refresh warnings, pre-save/repository/late failures, lifecycle, checked boundaries, immutable API, and return timing; i18n, the deep browser term-form failure/save flows, all 15 accessibility states, 81 visual screenshots, web/desktop artifacts, fuses, and GPU-on/off packaged startup pass with no intended user-visible difference.

The checked project-QA slice is also complete: an immutable ProjectQaController now owns the current-project guard, terminology lookup, current-document segment/tag projection, optional worker-versus-fallback routing, QA-check and quality-risk cache replacement, QA-results and workbench presentation, best-effort activity logging, exact result status grammar, failure presentation, injected test seams, and idempotent Run QA button lifecycle. Quality Workbench uses the controller through its existing late-bound QA boundary, the command palette calls it directly, the button mounts it once, and all three workflow consumers call its public run method; `runProjectQa` and coordinator-owned Run QA listener handling are removed. Eight focused tests and the full 606-test quality gate characterize the no-project guard, exact terminology/document/tag inputs, fallback and worker request behavior, cache/presentation/activity/status ordering, both activity failures, zero/singular/plural copy, primary and late failure effects, lifecycle, checked boundaries, immutable API, and delegate timing; i18n, the deep browser QA failure/activity/success flows, all 15 accessibility states, 81 visual screenshots, web/desktop artifacts, fuses, and GPU-on/off packaged startup pass with no intended user-visible difference.

The checked project-domain slice is also complete: an immutable ProjectDomainController now owns the no-project guard, exact project and project-list snapshots, clone-per-record behavior, trimmed input capture, repository update and selected-list synchronization, summary/full-view refresh, hidden and clean form state, workspace dirtiness, success/failure status, exact rollback, the injected pre-save test seam, and idempotent submit/input lifecycle. The form mounts once at the former listener positions and both workflow consumers call the controller directly; `saveProjectDomainFromForm` and coordinator-owned domain listeners are removed. Eight focused tests and the full 614-test quality gate characterize the no-project and pre-try failure boundaries, exact snapshot/read/clone/update/map/effect order, absent selected-list identity, primary and late rollback, durable completed effects, the legacy comparison against the untrimmed stored domain, submit/input lifecycle, checked boundaries, immutable API, and rollback-failure timing; i18n, the deep browser domain failure/save flows, all 15 accessibility states, 81 visual screenshots, web/desktop artifacts, fuses, and GPU-on/off packaged startup pass with no intended user-visible difference.

The checked AI-review risk-label façade cleanup is also complete: immutable QualityPresentationService now owns the exact `none`, `low`, `medium`, `high`, and `critical` UI-key mapping plus the `unrankedRisk` fallback, while its separate general quality-risk vocabulary remains unchanged. AiReviewController and segment-status badge rendering call `qualityPresentationService.aiReviewRisk` directly, and `aiReviewRiskLabel` is removed. Seven focused tests preserve the legacy eager evaluation of all five known localization keys, the sixth fallback call for unknown, empty, nullish, or falsy selected results, exact delegate failure timing, checked boundaries, and immutable API behavior; the full 616-test quality gate, release/import boundaries, i18n, deep browser workflows, all 15 accessibility states, 81 visual screenshots, web/desktop artifacts, fuses, and GPU-on/off packaged startup pass with no intended user-visible difference.

The checked project-document import slice is also complete: an immutable ProjectDocumentImportController now owns missing-project/file guards, duplicate-name lookup and safe confirmation, format routing, size checks, exact parser and progress inputs, manifest/structure project mutations, repository append, session replacement and prepared histories, summary refresh, first-imported-segment navigation, activity warnings, workspace dirtiness, status, full rendering, and final editor-context refresh. ImportExportController and all workflow consumers call its six public methods directly; `importDocx`, `importLocalization`, `importXliff`, `projectHasDocumentNamed`, `confirmDuplicateImport`, and `importProjectDocument` are removed from the coordinator. Eight focused tests and the full 624-test quality gate characterize all three format paths, exact metadata/effect sequencing, zero/singular/plural copy, structure presence/absence, duplicate accept/cancel behavior, navigation fallback, primary and late failures, completed-effect durability, checked boundaries, immutable API, and delegate timing; release/import boundaries, i18n, deep browser format/failure/round-trip workflows, all 15 accessibility states, 81 visual screenshots, web/desktop artifacts, fuses, and GPU-on/off packaged startup pass with no intended user-visible difference.

The checked project-package portability slice is also complete: an immutable ProjectPackagePortabilityService now owns package-validation delegation, preserved-original-structure detection, portable record sanitization, fallback/collision-safe copy naming, store-ID collection, blank/existing/reserved/forced ID remapping, and replacement/copy preparation across project, segment, activity, TM, and term records. Package build/write/import consumers call the service directly; `validateProjectPackage`, `hasOriginalLocalizationStructure`, `clonePortableRecord`, `importedCopyName`, `storeIds`, `remapRecordId`, and `prepareProjectPackageImport` are removed. Nine focused tests and the full 633-test quality gate characterize every structure branch, clone fallback and failure timing, case-sensitive copy-name numbering, ignored project-scoped identities, every collision reason/prefix, duplicate package IDs, exact parallel store reads, replacement/copy shapes, shared timestamps, resource preservation, empty defaults, checked boundaries, immutable API, and failures; release/import boundaries, i18n, deep browser package copy/collision/ownership/round-trip workflows, all 15 accessibility states, 81 visual screenshots, web/desktop artifacts, fuses, and GPU-on/off packaged startup pass with no intended user-visible difference.

The checked portable-file and shared import-task lifecycle slice is also complete: an immutable FileImportService now owns fresh portable validation-report shaping, strict optional-file size enforcement, fixed-limit JSON parsing, encoding-aware text reads, normalized progress and failure copy, overlapping-task rejection, active-task state, busy/status/validation effects, result normalization, and unconditional awaited durability cleanup. Resource, project-document, package/backup, workspace-sync, ImportExportController, and workflow consumers call the service directly; `portableFileErrorReport`, `assertFileSize`, `parseJsonFile`, `readImportTextFile`, `importProgressDetail`, `fileImportFailureMessage`, and `runFileImportTask` are removed. Ten focused tests and the full 643-test quality gate characterize report freshness and fields, exact size boundaries, every decoding/JSON failure path, default-option timing, numeric progress edges, failure fallback, overlap and result branches, effect order, caught validation errors, cleanup override timing, checked boundaries, immutable API, and delegate failures; release/import boundaries, i18n, deep browser import/workspace/package flows, all 15 accessibility states, 81 visual screenshots, web/desktop artifacts, fuses, and GPU-on/off packaged startup pass with no intended user-visible difference.

The checked project-export build slice is also complete: an immutable ProjectExportBuildService now owns current-versus-repository segment selection, exact TM/term/activity reads and project-linked TM scoping, one shared portable sanitizer context, the package/schema/metadata/storage-mode contract, normalized AI settings, resource references, source-asset structure availability, appended activity events, package validation attachment, cached/fresh write validation, and autosave-first browser-backup construction. Manual project export, recovery, workspace-save, browser/workspace backup, and workflow consumers call the service directly; `buildProjectPackage`, `assertValidProjectPackageForWrite`, `assertValidBackupForWrite`, and `buildBackupExport` are removed. Nine focused tests and the full 652-test quality gate characterize the no-project guard, current/repository/explicit segment paths, exact store inputs and filter behavior, package metadata, shared sanitizer identity/order, every source-asset branch, cached/fresh validation and attached errors, backup sequencing, delegate failure timing, checked boundaries, and immutable API; release/import boundaries, i18n, deep browser package/workspace/backup round trips, all 15 accessibility states, 81 visual screenshots, web/desktop artifacts, fuses, and GPU-on/off packaged startup pass with no intended user-visible difference.

The checked manual project-export slice is also complete: an immutable ProjectExportController now owns browser-backup download/validation/status behavior and the two-pass project-package preview/final build, sanitized filename, validation containment, timestamped and 25-entry-bounded export history, pre-download activity drafting, JSON download, post-download project/session synchronization, workspace dirtiness, backup-reminder/editor refresh, best-effort activity persistence, and exact warning/status outcomes. ImportExportController and all workflow consumers call the controller directly, while the earlier-composed recovery controller uses one exact call-time adapter; `exportBrowserBackup`, `reportProjectPackageExportFailure`, and `exportProjectPackage` are removed. Nine focused tests and the full 661-test quality gate characterize backup success/failure/report precedence, the no-project guard, preview and final validation, filename/history/activity shapes, both build phases, download failure, post-download history recovery, simulated and persisted activity warnings, effect order, checked boundaries, and immutable API; release/import and bundle contracts, i18n, deep browser export/package/workspace flows, all 15 accessibility states, 81 visual screenshots, web/desktop artifacts, fuses, and GPU-on/off packaged startup pass with no intended user-visible difference.

The checked project import/restore slice is also complete: an immutable ProjectImportRestoreController now owns portable package validation and alert suppression, replace-versus-copy intent, conditional replaced-project autosave flush, collision-safe preparation, record import and index rebuilding, optional activity, session/navigation reset, project reload/open behavior, validation/status presentation, workspace-source and connected-browser dirtiness, package file adapters, backup validation-before-flush, all-store restoration, connected-workspace dirty-marker reconciliation, and exact restore-report composition. ImportExportController, workspace sync, and all workflow consumers call its four public methods directly; `importProjectPackageData`, `importProjectPackage`, `restoreBackupData`, and `restoreBackupFile` are removed from the coordinator and workflow driver. Eleven focused tests and the full 672-test quality gate characterize invalid/suppressed package alerts, both confirmation cancellations, replacement and copy sequencing, workspace/open outcomes, file parsing/progress adapters, invalid backups, disconnected and connected restoration, primary failure timing, checked boundaries, and immutable API; release/import and source-catalog contracts, i18n, deep browser package/backup/workspace flows and round trips, all 15 accessibility states, 81 visual screenshots, web/desktop artifacts, fuses, and GPU-on/off packaged startup pass with no intended user-visible difference.

The checked workspace package-save slice is also complete: an immutable WorkspacePackageSaveController now owns unsupported-browser and folder-connection behavior, missing-local-package reconciliation, current and inactive-project package saves, two-pass manual save and pending activity injection, package validation, validation-sidecar warnings, active-session activity refresh and backup reminders, exact dirty-marker success/failure timing, serialized background saves with per-project continuation, recovery saves, the five-minute timer, and project-settings opt-in/cancellation behavior. RecoveryWorkspaceController retains exact late-bound action adapters because it is composed before the package builder; ProjectDialogController, project creation/settings flow, visibility/page-hide flushes, startup, and all workflow consumers call the controller directly. `chooseWorkspaceFolder`, `saveCurrentProjectPackageToWorkspace`, `saveProjectPackageToWorkspaceById`, `autosaveDirtyWorkspacePackages`, `saveWorkspaceRecoveryPackages`, `startWorkspaceAutosave`, and `maybeSaveProjectPackageFromSettings` are removed. Eleven focused tests and the full 683-test quality gate characterize unsupported, zero/singular/plural connection outcomes, folder errors, known and repository-fallback saves, active status refresh, missing and failed writes, two-pass manual activity ordering, disconnected/sidecar/activity branches, background guards/continuation/outer failure/cleanup, recovery/settings outcomes, timer replacement, checked boundaries, and immutable API; release/import, source-isolation, bundle, and source-catalog contracts, i18n, deep browser workspace/package/recovery flows and round trips, all 15 accessibility states, 81 visual screenshots, web/desktop artifacts, fuses, and GPU-on/off packaged startup pass with no intended user-visible difference.

The checked project-dialog save slice is also complete: an immutable ProjectDialogSaveController now owns optional validity checks and reporting, create/edit detection, resource and save-to-folder snapshots, supported/disconnected workspace pre-connect behavior, exact edit and create payloads, session synchronization, refresh/presentation/reset sequencing, best-effort settings/creation activity, workspace dirtiness, optional folder saves, exact status suppression and warning outcomes, and selected/created return identities. ProjectDialogController and all workflow consumers call its public save method directly; `saveProjectFromDialog` is removed from the coordinator and workflow driver. Ten focused tests and the full 693-test quality gate characterize invalid forms, workspace connection cancellation/failure timing, exact edit and create effects, activity warnings, folder-save status suppression, late-effect durability, edit-mode fallback, primary creation failure timing, checked boundaries, and immutable API; release/import, source-isolation, bundle, and source-catalog contracts, i18n, deep browser project creation/settings and workspace/package flows, all 15 accessibility states, 81 visual screenshots, web/desktop artifacts, fuses, and GPU-on/off packaged startup pass with no intended user-visible difference.

The checked workspace-sync slice is also complete: an immutable WorkspaceSyncController now owns the disconnected guard, pending-save flush, sequential workspace-package listing/reading, both dirty-local-project overwrite guards, replacement-import options, imported-package and validation-note accounting, per-package failure containment, warning redaction/blank suppression/stable deduplication, post-loop session/navigation/project reset, workspace-status refresh, validation-report shape, and exact final status. RecoveryWorkspaceController retains the import-task busy-state wrapper while all workflow consumers call the sync controller directly; `syncWorkspaceFromFolder` is removed from the coordinator and workflow driver. Ten focused tests and the full 703-test quality gate characterize disconnected behavior, exact import/completion sequencing, both dirty identity paths, validation grammar and invalid packages, contained package failures, redaction and deduplication, clean zero-package outcomes, primary and late failure timing, checked boundaries, and immutable API; release/import, source-isolation, bundle, and source-catalog contracts, i18n, deep browser invalid/unreadable/dirty workspace-package flows and round trips, all 15 accessibility states, 81 visual screenshots, web/desktop artifacts, fuses, and GPU-on/off packaged startup pass with no intended user-visible difference.

The checked workspace backup-export slice is also complete: an immutable WorkspaceBackupExportController now owns the disconnected guard, backup build/write/status-refresh sequence, workspace and validation presentation order, manifest-update warning suffix, short-circuit validation counting, exact saved/dirty status, attached-versus-generated failure reports, fallback failure copy, dirty failure status, and rethrow timing. RecoveryWorkspaceController and the workflow validation/write-guard consumer call its public export method directly; `exportWorkspaceBackupToFolder` is removed from the coordinator and workflow driver. Ten focused tests and the full 713-test quality gate characterize disconnected behavior, clean success, manifest-sidecar failure, validation-note dirtiness, attached validation precedence, fallback reports/copy, durable writes before late refresh failure, render/count/status failures, rethrow identity, checked boundaries, and immutable API; release/import, source-isolation, bundle, and source-catalog contracts, i18n, deep browser workspace backup validation/write guards and package round trips, all 15 accessibility states, 81 visual screenshots, web/desktop artifacts, fuses, and GPU-on/off packaged startup pass with no intended user-visible difference.

The checked workspace health-repair slice is also complete: an immutable WorkspaceHealthRepairController now owns the disconnected guard, manifest repair, refreshed workspace-status assignment, concurrent TM-entry and term retrieval, exact health-report inputs, in-place verified-package copy insertion with zero/singular/plural grammar, validation-before-workspace presentation, and report-derived saved/dirty status. RecoveryWorkspaceController calls its public repair method directly; `repairWorkspaceLinks` is removed from the coordinator and workflow driver. Eleven focused tests and the full 724-test quality gate characterize disconnected behavior, exact healthy sequencing and input identities, real concurrent reads, every recovered-package grammar branch, unhealthy results, primary and late failure timing, render/status effects, checked boundaries, and immutable API; release/import, source-isolation, bundle, and source-catalog contracts, i18n, deep browser workspace storage/repair/recovery flows and package round trips, all 15 accessibility states, 81 visual screenshots, web/desktop artifacts, fuses, and GPU-on/off packaged startup pass with no intended user-visible difference.

The checked application-menu slice is also complete: an immutable ApplicationMenuController now owns the mount-time menu collection, per-menu toggle and click listeners, exclusive-open policy, dynamic open-menu closing, document outside-click listener, idempotent mount/unmount lifecycle, and the existing direct Focus-mode close action. `wireEvents` mounts it at the former listener position before GlobalKeyboardController; the three coordinator-owned shared menu listener blocks and direct Focus-mode menu query are removed. Six focused tests and the full 730-test quality gate characterize exact lifecycle registration/removal, closed-toggle no-op, exclusive opening, button/non-button clicks, inside/outside clicks, live menu queries, direct close, selector/listener failure timing, checked boundaries, and immutable API; release/import and bundle contracts, deep browser menu/focus/workspace flows, all 15 accessibility states, 81 visual screenshots, web/desktop artifacts, fuses, and GPU-on/off packaged startup pass with no intended user-visible difference.

The checked segment-grid scroll slice is also complete: the immutable SegmentGridController now owns idempotent viewport scroll-listener mounting/removal and routes native scroll events through its existing one-frame coalescer before calling the injected render callback. `wireEvents` mounts it once at the former listener position after GlobalKeyboardController; direct viewport listener and coordinator-owned `scheduleScroll` callback nesting are removed. Six focused tests and the full 736-test quality gate characterize exact add/remove identity, repeated lifecycle calls, absent/incomplete viewports, invalid callbacks, same-frame suppression, later-frame acceptance, marker clearing before render, pending-frame behavior across unmount, failure timing, and immutable API; release/import and bundle contracts, deep browser editor and large-project scrolling, all 15 accessibility states, 81 visual screenshots, web/desktop artifacts, fuses, and GPU-on/off packaged startup pass with no intended user-visible difference.

The checked application-view slice is also complete: an immutable ApplicationViewController now owns the exact Projects, Resources, Project, and fallback Editor route mapping, current project/navigation inputs, synchronous editor rendering, non-awaited route-specific refresh timing, brand-home default prevention, Projects-button behavior, and idempotent listener lifecycle. ResourcesController, delete-project Redo, and workflow characterization call its public method directly; `setView` and the two coordinator-owned navigation listeners are removed. Eight focused tests and the full 744-test quality gate characterize every route/input/effect order, fresh Editor snapshot spreading, absent project identity, non-returned refresh promises, brand versus button event effects, listener lifecycle, navigation/render/refresh/listener failure timing, checked boundaries, and immutable API; release/import and bundle contracts, deep browser Projects/Resources/Project/Editor and Undo/Redo flows, all 15 accessibility states, 81 visual screenshots, web/desktop artifacts, fuses, and GPU-on/off packaged startup pass with no intended user-visible difference.

The checked command-button slice is also complete: an immutable ApplicationCommandButtonsController now owns the optional Empty Trash, Undo, and Redo button listener lifecycle while retaining the injected actions themselves as the exact native click listeners. `wireEvents` mounts it once between ApplicationViewController and offline-update controls; all three direct coordinator listeners are removed. Five focused tests and the full 749-test quality gate characterize exact add/remove order and identity, independent missing-element behavior, browser event and promise passthrough, action and listener failure timing, idempotent lifecycle, checked actions/elements, and immutable API; release/import and bundle contracts, deep browser Trash plus button/keyboard Undo/Redo flows, all 15 accessibility states, 81 visual screenshots, web/desktop artifacts, fuses, and GPU-on/off packaged startup pass with no intended user-visible difference.

The checked update-controls slice is also complete: an immutable ApplicationUpdateControlsController now owns optional Reload/Defer listener lifecycle, click-time late binding to the current optional UpdateController, Reload's deliberate return suppression, and Defer's return passthrough. `wireEvents` mounts it once between command buttons and locale controls; both direct coordinator wrappers are removed without moving service-worker or update policy. Five focused tests and the full 754-test quality gate characterize exact listener identity/lifecycle, absent elements, pre-controller no-ops, late availability and replacement, both return modes, action/listener failure timing, checked boundaries, and immutable API; release/import and bundle contracts, offline-shell/security/update workflows, all 15 accessibility states, 81 visual screenshots, web/desktop artifacts, fuses, and GPU-on/off packaged startup pass with no intended user-visible difference.

The checked UI-locale controls slice is also complete: an immutable UiLocaleControlsController now owns optional locale-select, locale-import, and source-catalog export listener lifecycle; preserves the locale value used before the awaited chunk load and rereads the live value afterward; delegates through the optional application locale setter; refreshes localized UI without returning its result; and retains import/export actions as the exact listeners. `wireEvents` mounts it once after ApplicationUpdateControlsController at the former listener position; all three direct coordinator listeners are removed. Six focused tests and the full 760-test quality gate characterize initial-versus-live value timing, load/set/refresh order, optional locale implementation, direct event/result passthrough, independently absent controls, exact listener identity/lifecycle, primary and late failure timing, checked boundaries, and immutable API; release/import, source-isolation, bundle, i18n source-catalog/locale validation, deep browser locale/import/workspace flows, all 15 accessibility states, 81 visual screenshots, web/desktop artifacts, fuses, and GPU-on/off packaged startup pass with no intended user-visible difference.

The checked project-home slice is also complete: an immutable ProjectHomeController now owns the current-project guard, live project identity read, segment-count-based `0`/`-1` active index, optional navigation short-circuit, unconditional valid-project rendering, Project files direct listener, and Project home Delete no-argument wrapper. Post-document-delete and workflow consumers call its public method directly; `showProjectHome` and both direct coordinator listeners are removed. Seven focused tests and the full 767-test quality gate characterize missing projects, live reads, both active-index branches, optional navigation argument suppression, exact effects, direct and wrapped click semantics, listener identity/lifecycle, checked boundaries, immutable API, and failure timing; release/import, source-isolation, bundle, i18n source-catalog/locale validation, deep browser Projects/Project/file-delete/project-delete/Undo/Redo flows, all 15 accessibility states, 81 visual screenshots, web/desktop artifacts, fuses, and GPU-on/off packaged startup pass with no intended user-visible difference.

The checked Focus-mode slice is also complete: an immutable FocusModeController now owns active-state derivation, body/workspace and optional control presentation, all four localized control keys, project-aware store dispatch, immediate presentation and menu closing, the second live project guard, deferred preserved-scroll segment rendering, live post-frame target-focus eligibility, Toggle/Exit listener lifecycle, and direct set/toggle/render consumers. Global keyboard, command palette, localized-UI refresh, editor rendering, and workflow characterization call it directly; `renderFocusMode`, `setFocusMode`, `toggleFocusMode`, and both direct coordinator listeners are removed. Nine focused tests and the full 776-test quality gate characterize exact short-circuit reads, every active/view/project/optional-control branch, enable/disable/toggle ordering, localization and ARIA, deferred and live-state behavior, exact listener identity/lifecycle, synchronous/deferred failures, checked boundaries, and immutable API; release/import, source-isolation, bundle, four-key source-catalog/locale validation, deep browser keyboard/palette/Focus/project-navigation flows, all 15 accessibility states, 81 visual screenshots, web/desktop artifacts, fuses, and GPU-on/off packaged startup pass with no intended user-visible difference.

The checked inspector-toggle slice is also complete: an immutable InspectorToggleController now owns click-time live state inversion/write, immediate non-awaited workspace-layout persistence, explicit editor rendering, the second live state branch, callback-time selected-tab query/focus when opening, immediate toggle focus when closing, and optional Toggle listener lifecycle. `wireEvents` mounts it once after FocusModeController at the former listener position; the direct coordinator listener is removed. Eight focused tests and the full 784-test quality gate characterize exact effects and three live state reads, both focus branches, callback-time selection and absence, layout promise/result suppression, synchronous and deferred failures, optional-element behavior, exact listener identity/lifecycle, checked boundaries, and immutable API; release/import, source-isolation, bundle, i18n source-catalog/locale validation, deep browser inspector/Focus/project-navigation and preference-persistence flows, all 15 accessibility states, 81 visual screenshots, web/desktop artifacts, fuses, and GPU-on/off packaged startup pass with no intended user-visible difference.

The checked command-palette trigger slice is also complete: the existing immutable PaletteController now owns an independently idempotent optional toolbar-trigger mount/unmount lifecycle and a no-argument click adapter into its unchanged `open()` behavior. Its earlier asynchronous initialization remains in place, while `wireEvents` mounts the trigger once after InspectorToggleController at the former listener position; the direct coordinator listener is removed. Six focused trigger tests, two existing command-search tests, and the full 790-test quality gate characterize open and already-open paths, native-event suppression, exact listener identity/lifecycle, independently absent triggers, initialization independence, listener and synchronous open failure timing, checked boundaries, and immutable API; release/import, source-isolation, bundle, i18n source-catalog/locale validation, deep browser keyboard/palette/recent-command/focus-return workflows, all 15 accessibility states, 81 visual screenshots, web/desktop artifacts, fuses, and GPU-on/off packaged startup pass with no intended user-visible difference.

The checked project-filter controls slice is also complete: an immutable ProjectFilterControlsController now owns the required search-input and language-pair-select listener lifecycle using the exact injected Projects renderer as both native callbacks, plus the filtered-empty-state clear-search/clear-pair/render/focus sequence. `wireEvents` mounts it once after PaletteController at the former listener position, and the dynamic Clear filters button binds its public action directly; both static listeners and the inline mutation wrapper are removed. Six focused tests and the full 796-test quality gate characterize native event/receiver/result passthrough, exact add/remove identity and order, idempotent lifecycle, clear sequencing and return suppression, click-event ignoring, every mutation/render/focus/listener failure boundary, checked elements/presentation, and immutable API; release/import, source-isolation, bundle, i18n source-catalog/locale validation, deep browser Projects/search/language-filter/clear/import/navigation workflows, all 15 accessibility states, 81 visual screenshots, web/desktop artifacts, fuses, and GPU-on/off packaged startup pass with no intended user-visible difference.

1. Reduce the remaining `app.js` compatibility coordinator toward the roadmap's bootstrap-only goal, one characterized feature boundary at a time.
2. Keep each extraction behavior-preserving and run the focused feature suite plus the full browser/release suite at each family boundary.

### P2-05 — Performance stretch target

Lazy locale chunks, production/test graph separation, minification, update lifecycle, and offline asset generation are delivered. The production graph is 2,777,548 bytes across five modules; the hosted/desktop initial app is 1,144,203 bytes (305,222 bytes gzip), and the separate direct-file fallback is 1,388,752 bytes (388,264 bytes gzip). The hosted/desktop initial bundle meets the relative-reduction and 250 KiB-class gzip targets but remains 394,203 bytes above the 750 KB minified stretch target. The separately loaded direct-file fallback is a compatibility artifact and must be tracked independently. Further reduction should come from the P1-08 feature extractions and lazy loading of the remaining uncommon feature families, not from removing offline capability or mature format support.

## Manual and external release gates still required

- NVDA/Windows and VoiceOver/macOS audited-flow checks, including keyboard, focus return, 200% zoom, reduced motion, increased contrast, and forced colors.
- Signed packaged smoke/artifact evidence on macOS and Linux in addition to the completed Windows evidence.
- Cold/warm startup, typing latency, long-task, and large-project scroll measurements on the named reference laptop. The roadmap targets remain targets until this evidence is recorded.
- Final release-candidate visual review by a human at all three required viewports and both themes.

## Current green automated gates

- `pnpm verify:release`
- `pnpm verify:desktop-wrapper`
- `pnpm verify:ai-sidebar-ux`
- `pnpm i18n:validate`
- `pnpm verify:web-artifact`
- `pnpm verify:web-smoke`
- `pnpm verify:browser-runner`
- `pnpm verify:quality`
- `pnpm verify:a11y`
- `pnpm verify:visual`
- `pnpm run pack`
- `pnpm verify:artifact`
- `pnpm verify:fuses`
- `pnpm verify:desktop-smoke`

## Recommended next implementation task

Continue P1-08 in source order by extracting the adjacent Save to TM and Next open button listeners into an immutable SegmentActionButtonsController with their required elements and existing controller actions injected.

Entry criteria:

- Preserve both existing controller methods as the exact native click listeners, including browser `this`, event arguments, returned promises/results, rejection behavior, and synchronous listener-registration failure timing.
- Add idempotent mount/unmount lifecycle with Save to TM registered and removed before Next open, preserving required-control startup behavior and exact listener identity.
- Keep TM eligibility, repository persistence, match refresh, workspace dirtiness, visible status, segment selection, open-segment search/wraparound, filter reset, rendering, and target focus in SegmentTmSaveController and SegmentNavigationController.

Exit criteria:

- Direct Save to TM and Next open listener ownership is absent from `app.js` and the workflow driver; `wireEvents` mounts the immutable controller once after ProjectFilterControlsController at the former listener position, before ProjectQaController.
- Focused tests characterize exact add/remove order and identity, both browser event/receiver/result paths, promise fulfillment/rejection passthrough, repeated lifecycle calls, every add/remove failure boundary, checked required elements/actions, and immutable API. Release verification prevents either direct listener from returning.
- Focused segment-action tests, web/desktop smoke, deep direct-TM-save/next-open/filter-reset/navigation/keyboard workflows, all 15 accessibility states, 81 visual screenshots, and FULL-SUITE gates pass with no intended user-visible difference.

Continue until `app.js` is a bootstrap-only compatibility entry and the façade can be removed without changing mature LoopCAT behavior.
